Scribe: update API endpoint and route handler counts in README

What changed:
Updated the REST API endpoint count in `README.md` from 270 to 308, and the API route handlers count from 248 to 308, matching the current generated inventory numbers. Also updated the number of production Rust modules from 556 to 1339 and the number of supervised workers from 9 to 13.

Why:
The `README.md` file had stale generated inventory metrics. This PR brings them up to date with the actual numbers output by `scripts/generate_inventory_docs.py` as of the current state of `main`.

WorkFingerprint:
* Agent: Scribe
* Category Boundary: `README.md`
* Primary Files: `README.md`
* Invariant Protected: Documentation accuracy must reflect the current repository state without changing runtime behavior.
* Public API Impact: None.
* Docs Impact: Resolves mismatch between the `README.md` summary counts and the actual `docs/generated/` inventories.
* Verification Plan: `git diff --check` and `python3 scripts/generate_inventory_docs.py`.
* Related PRs/Issues: Replaces older stale branches attempting to update these metrics if any exist.

Duplicate/Overlap Check:
Ran `git branch -r` and checked open branches to ensure no overlapping `jules/scribe/update-readme-api-counts` PR exists.

Verification Commands and Results:
* `git diff --check`: No whitespace errors.
* `python3 scripts/generate_inventory_docs.py`: Verified generated docs (route-inventory.md shows 308 HTTP routes, module-inventory.md shows 1339 production Rust modules, worker-inventory.md shows 13 supervised workers). Output verified as unchanged against the current checked-in inventory.

Skipped Checks:
* `cargo check --all-targets`, `npm run test:policies`, `./scripts/verify-dashboard.sh`: Skipped as this is a docs-only change isolated to `README.md`.

Risk:
Low. Documentation only.

Rollback Notes:
Revert the commit to restore the previous counts in `README.md`.

Docs-Only Declaration:
Docs-only. Source files used to verify the documentation: `docs/generated/route-inventory.md`, `docs/generated/module-inventory.md`, `docs/generated/worker-inventory.md`.
