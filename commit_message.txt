Doctor: clarify Qwen auth diagnostic guidance

What changed:
Updated the guidance string in `check_qwen_auth_hints` to point users to the DashScope web console for usage limits and removed the hallucinated instruction to run `/stats` in the Qwen CLI (which doesn't exist).

Why:
Operators using the Aliyun DashScope (Qwen) provider were being presented with confusing and incorrect guidance when troubleshooting auth. The old message told them to use a CLI tool to check `/stats`, which is an invalid workflow. The updated message explicitly instructs them to check the DashScope web console.

WorkFingerprint:
- Agent: Doctor
- Boundary: src/cli/doctor/orchestrator.rs
- Primary files: src/cli/doctor/orchestrator.rs
- Invariant protected: Doctor behavior should inspect and report; preserve JSON output contracts.
- Related PRs/issues: None open locally.

Duplicate check:
Checked open PRs, no overlaps with `check_qwen_auth_hints` modifications.

Verification:
- `cargo check --all-targets` passed
- `cargo test --bin agentdesk -- orchestrator::tests::qwen` passed

Skipped checks:
None.

Risks:
Low. Output string update for non-mutating doctor CLI.

Rollback notes:
Revert the single string commit in `src/cli/doctor/orchestrator.rs`.
