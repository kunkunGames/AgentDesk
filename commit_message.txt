ApiRoutemaster: add route-level tests for hooks domain boundary

What changed: Added route-level tests in `src/server/routes/hooks.rs` to verify that hooks endpoints are correctly protected by the domain auth middleware and respond appropriately (UNAUTHORIZED or SERVICE_UNAVAILABLE) when PG is missing.

Why: PR #203 moved hooks routes to the integrations domain but failed to add route-level tests or compile checks. The generated PR branch became stale with unrelated changes, so this recreates the test requirement against main as requested by the AgentDesk PR review lessons.

WorkFingerprint:
- Agent: ApiRoutemaster
- Category Boundary: API routing or domain-boundary improvements
- Primary Files: `src/server/routes/hooks.rs`
- Invariant Protected: Domain auth and path mount correctness for /hook/*
- Public API Impact: None (tests only)
- Docs Impact: None
- Verification Plan: Compile test and verify no overlapping PRs.
- Duplicate Check: `git log --all --grep="ApiRoutemaster:"` (none matched current hook move active PR)

Verification Commands:
- `cargo check --lib --tests` (Passed)
- Skipped `cargo test --all-targets` because the container OOMs/times out when compiling all tests for this app; `cargo check` validates the module and domain wiring.

Risk: Low. Test-only addition.
Rollback: Drop the `#[cfg(test)]` block.
