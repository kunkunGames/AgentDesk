Refiner: reuse runtime_layout::expand_user_path in voice_barge_in

What changed:
Updated the local `expand_tilde` helper in `src/services/discord/voice_barge_in.rs` to route through the centralized `crate::runtime_layout::expand_user_path` helper for tilde expansion.

Why:
Consolidates tilde expansion behavior to reduce drift risk and align with earlier PR review feedback (such as PR #212) that explicitly replaced duplicate path expansions with the canonical `runtime_layout::expand_user_path`.

WorkFingerprint:
- Agent: Refiner
- Category boundary: Rust backend, voice barge in module
- Primary files: `src/services/discord/voice_barge_in.rs`
- Invariant protected: Fallback behavior stays byte-for-byte equivalent for non-tilde paths, and accurately expands home paths without `dirs::home_dir()` drift.
- Public API / Docs impact: None
- Related issues: None

Duplicate/overlap check:
Checked open PRs (using `git log origin/upstream-pr/...` and `git branch -r`) to confirm no overlapping changes affect the `expand_tilde` fallback in `voice_barge_in.rs`. Used `jules/refiner/reuse-voice-utils-expand-tilde-barge-in` since another agent had opened a dedupe PR previously.

Verification commands:
- `cargo check --lib` (passed)

Skipped checks:
- `cargo test --lib services::discord::voice_barge_in` (skipped due to OOM limit during linking in current test environment, compilation works)

Risk and rollback notes:
Risk is low as the same validation logic and fallback to `path.to_path_buf()` is retained. Revert `voice_barge_in.rs` if issues arise.
