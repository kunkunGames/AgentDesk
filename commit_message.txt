WorkerRegistry: add target observability to worker supervision start/skip logs

What changed:
Added the `observability_target` field mapping to `spec.target` on worker lifecycle log sites in `src/server/worker_registry.rs`.
Also updated worker inventory using the document generator script.

Why:
Extends worker supervision observability consistency applied in #193.
Currently `observability_target` is absent in logs generated by `record_worker_local_terminal_signal`, `record_leader_only_worker_started`, `record_leader_only_worker_stopped`, `supervise_leader_tokio_worker`, `log_start`, `log_skip` etc.

WorkFingerprint:
WorkerRegistry, src/server/worker_registry.rs, worker supervision observability logging, added `observability_target` on lifecycle log messages, verified using cargo check, docs regen.

Duplicate/Overlap check:
Verified overlapping PR #193 and related overlap-reports. This change implements the original requirement on a clean branch off main, fulfilling the queue debt while maintaining a minimal diff.

Verification:
- `cargo check --all-targets` succeeds (compilation works).
- `git diff --check` succeeds.
- `python3 scripts/generate_inventory_docs.py` ran.

Skipped checks:
- `cargo test --bin agentdesk server::worker_registry` timed out/SIGKILL due to memory/CPU limitations on runner, compilation was verified with check.

Risk & Rollback:
Risk is low as it's purely adding a field to tracing logs. Rollback via reverting this commit.
