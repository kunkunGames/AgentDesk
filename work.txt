IntakeRouter PR: Accurate telemetry for node override routing

Files:
src/services/cluster/intake_router_hook.rs
src/services/cluster/intake_routing_telemetry.rs

Changes:
- Add `basis: IntakeRoutingBasis` to `ObservedIntakeOutcome::WouldAssignNoOwnerToTarget` variant.
- Populate `basis` when constructing this variant in `src/services/cluster/intake_router_hook.rs` (using `IntakeRoutingBasis::NodeOverride` for node override, and `IntakeRoutingBasis::PreferredLabels` for preferred labels).
- Use `basis` in `src/services/cluster/intake_routing_telemetry.rs` to set `preferred_label_match: PreferredLabelMatchCode::MatchedWorker` for `IntakeRoutingBasis::PreferredLabels`, and `PreferredLabelMatchCode::NotEvaluated` otherwise.
- Update tests to populate `basis`.

The existing branch `origin/jules/intake-router/telemetry-node-override-labels-7036183096749534308` seems to contain this exact change but the git tree was corrupted and has a huge diff.

The other PR is `origin/jules/intake-router/fix-node-override-dup-log-11347358787839837809` which modifies the log string "[intake_router] duplicate Discord message (node override) — existing row already covers it; skipping local execution" to dynamically show whether it's a node override or preferred labels.
