# 904-7 — API Overhaul Audit & Migration Report

**Issue:** #1069 (campaign R4)
**Scope:** Audit residual callsites for the API path/parameter renames landed in
#1064 / #1065, migrate any remaining hits, and lock in regression coverage with
a static-text smoke test.

## Migrations performed

| Category | Files touched | Migration |
|----------|---------------|-----------|
| Public docs (path table) | `FEATURES.md` | `/api/auto-queue/activate` → `/api/auto-queue/dispatch-next` (with `formerly` annotation) |
| Auto-queue lifecycle diagram | `docs/diagrams/03-auto-queue-lifecycle.md` | Same as above (PlantUML node label) |
| Server doc-comments (legacy path mention) | `src/server/routes/auto_queue.rs` (3 sites: route doc + 2 success-message strings) | `/api/auto-queue/activate` → `/api/auto-queue/dispatch-next` (response body now points clients at the canonical endpoint) |
| Server doc-comments (legacy path mention) | `src/server/routes/kanban.rs` | `/api/re-review` → `/api/kanban-cards/batch-rereview` (route doc-comment) |

**Total concrete migrations:** 5 files, 6 sites.

## Audited (clean — no migration needed)

- `dashboard/src/**` — already migrated by #1064/#1065. The single `/api/discord-bindings` reference is a comment explicitly noting the legacy fallback is still accepted (deprecation marker, not a fetch URL).
- `policies/**` — no live URL strings to legacy paths.
- `scripts/**` — `scripts/queue-stability-batch.sh` already migrated to `/dispatch-next` in #1064.
- `skills/**` — only `memory-merge`, `memory-read`, `memory-write` skills present; none reference any HTTP API path.
- `agentdesk.example.yaml` — only generic `memory:` and `*_sec` config keys; no HTTP paths.
- `CLAUDE.md` (project) — points to `docs/source-of-truth.md`; no API paths.

## Knowingly retained legacy references

These references must remain — removing them would break backward-compat or contradict the audit history:

| File | Reason |
|------|--------|
| `src/server/routes/auth.rs` | `hook/*` prefix bypass for `/api/hook/session/{sessionKey}` (parameterized DELETE preserved per #1064). |
| `src/server/routes/discord.rs` (line 15) | Doc-comment explaining `/api/discord-bindings` is a kept-as-deprecated alias (#1065). |
| `src/server/routes/domains/integrations.rs` | `log_deprecated_alias("/api/discord-bindings", ...)` runtime warning. |
| `src/server/routes/docs.rs` | Inventory enumerator must list legacy aliases so dashboards can render the `deprecated` flag. |
| `src/server/routes/routes_tests.rs` (existing `removed_legacy_routes_return_not_found`) | Asserts the legacy POSTs return 404/405 — string list is the contract. |
| `docs/api-overhaul/proposal-2026-04-22.md` | Historical proposal table (before/after). |
| `docs/generated/route-inventory.md` | Auto-generated by `scripts/generate_inventory_docs.py`; will refresh on next run. |
| `src/cli/client.rs:638` (`/api/auto-queue/dispatch`) | Functional alias deliberately retained per #1064 — `cmd_dispatch` still ships the `groups` body shape; full migration to `/generate` + `/dispatch-next` is tracked separately. |
| `README.md` JS `cardId` (SDK example) | JavaScript SDK function-parameter name (camelCase) is the surface, not the HTTP body. |

## Memento (informational)

Memento `permanent` and `workspace` categories are operator-managed; this audit
only flags the migration so the operator can run the standard review pass.
Recommended check (no auto-mutation):

```text
recall keywords=["/api/re-review", "/api/auto-queue/activate", "/api/discord-bindings", "assigned_agent_id (request body)"]
```

Each hit should be `amend`-ed or `forget`-ed manually.

## Smoke test

Added: `src/server/routes/routes_tests.rs::callsites_migrated_off_legacy_api_paths_1069`

- Walks the dashboard frontend, policies, scripts, skills, and top-level config / docs roots.
- Greps for the three banned substrings (`/api/re-review`, `/api/auto-queue/activate`, `/api/discord-bindings`).
- Allows comment / markdown lines that explicitly reference the migration history (`#1064`, `#1065`, `removed`, `legacy`, `formerly`, `deprecated`, arrow markers).
- Fails if a new callsite re-introduces a legacy path.

Run:

```bash
cargo test --bin agentdesk callsites_migrated_off_legacy_api_paths_1069
```

Result on this branch: **passing** (cargo test: 1 passed, 2482 filtered out).

## Prompt simplification (memory/issue branching)

Per the campaign brief, prompts should now describe a single API surface. The
project `CLAUDE.md` already delegates to `docs/source-of-truth.md` and contains
no memory/issue branching logic to remove. Skill prompts (`memory-merge`,
`memory-read`, `memory-write`) operate on memory files and never describe HTTP
paths, so they remain canonical. **No prompt edits required.**

## Remaining TODO

- `src/cli/client.rs::cmd_dispatch` migration to `/generate` + `/dispatch-next`
  (deferred from #1064 due to body-shape mismatch). Tracked in the campaign
  backlog.
- Re-run `python3 scripts/generate_inventory_docs.py` once the unrelated
  `top-level architecture map drift: missing descriptions for compat/` failure
  is fixed; this will refresh `docs/generated/route-inventory.md` to drop the
  legacy rows from the rendered table.

## Categories — file count summary

| Category | Files audited | Files migrated |
|----------|---------------|----------------|
| Skill bodies (`skills/*/SKILL.md`) | 3 | 0 |
| Memento permanent/workspace memories | operator-only | 0 (informational) |
| `CLAUDE.md` (global + project) | 2 | 0 |
| `agentdesk.yaml` / `agentdesk.example.yaml` | 1 | 0 |
| Dashboard frontend (`dashboard/src/**`) | ~250 | 0 (already migrated) |
| Public docs (`FEATURES.md`, diagrams) | 2 | 2 |
| Server route doc-comments | 2 | 2 |
| **Total migrated** | — | **4** |
