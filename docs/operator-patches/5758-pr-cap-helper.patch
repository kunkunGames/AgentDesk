diff --git a/agentdesk-issue-pipeline/SKILL.md b/agentdesk-issue-pipeline/SKILL.md
--- a/agentdesk-issue-pipeline/SKILL.md
+++ b/agentdesk-issue-pipeline/SKILL.md
@@ -256,3 +256,4 @@
 - **간단 수정 예외**: 회귀 반경이 좁은 단일 관심사 소형 diff(테스트/문서/포맷/명백한 단일-사이트 픽스)이고 핫파일·relay-state-contract·migration·배달 권위·live-turn 비접촉이면 카운터리뷰 1 leg로 축소 가능. 모호하면 dual.
-- **PR 크기 상한: 20파일 / 추가줄 +800(삭제줄 제외).** raw diff로 먼저 측정하고, 초과하면 **리뷰를 태우기 전에 분할한다.** 큰 PR은 리뷰가 길어지고 → base가 낡고 → rebase 충돌이 나고 → 다시 리뷰로 돌아가는 루프를 만든다. CI가 커밋 동기화를 강제하는 생성물 또는 분할 시 알려진 결함이 먼저 착지하는 불가분 변경만 예외다. 예외 시 “캡 안”으로 재정의하지 말고 PR 본문에 raw 수치·리뷰어가 읽는 표면·불가분 사유를 함께 명시한다(2026-07-31 실측: #5005 **68파일 +6836/−498**, #5022 발산 브랜치 **52파일 +2250/−1222**).
+- **PR 크기 상한: 20파일 / 추가줄 +800(삭제줄 제외).** `scripts/pr_cap_check.sh <branch-or-sha>`로 먼저 측정하고(생략 시 HEAD), 초과하면 **리뷰를 태우기 전에 분할한다.** 큰 PR은 리뷰가 길어지고 → base가 낡고 → rebase 충돌이 나고 → 다시 리뷰로 돌아가는 루프를 만든다. CI가 커밋 동기화를 강제하는 생성물 또는 분할 시 알려진 결함이 먼저 착지하는 불가분 변경만 예외다. 예외 시 “캡 안”으로 재정의하지 말고 PR 본문에 raw 수치·리뷰어가 읽는 표면·불가분 사유를 함께 명시한다(2026-07-31 실측: #5005 **68파일 +6836/−498**, #5022 발산 브랜치 **52파일 +2250/−1222**).
+  - 대상 저장소 안에서 실행한다. 매번 origin의 main을 fetch한 merge-base 기준이며, 삭제 credit 0이다. fetch/ref/diff 오류 또는 binary 줄 수 미확인은 통과가 아니다. 미커밋 변경은 포함하지 않는다. 상세 측정 의미는 리포 `docs/pr-cap-check.md`를 따른다.
   - **★ 모든 구현·수리 브리프는 예산 세 숫자를 반드시 싣는다 — 없으면 발사 금지.** ① 현재 표면 실측(비테스트 파일수/추가줄) ② 남은 캡 여유 ③ **이번 라운드 예산 상한**. 숫자가 없으면 레인은 캡을 "아직 안 넘었다"로만 읽고 리뷰 지적을 전부 새 코드로 흡수한다.
