# Source Of Truth

This document answers one question: when the same knowledge appears in repo files, runtime files, provider config, or legacy backups, which file do we edit?

The rules below are derived from the current runtime/layout code in `src/runtime_layout/paths.rs`, `src/services/discord_config_audit.rs`, `src/services/mcp_config.rs`, and the release copy flow in `scripts/deploy-release.sh`.

The runtime and operator paths in this index were verified on 2026-04-24 with `ls`, `find`, `plutil`, and targeted `rg` checks. If a path is absent on another host, recreate it through the documented init/deploy/repair workflow instead of inventing a second canonical location.

The orchestration rule-surface rows (lane caps, campaign execution rules) were added on 2026-09-11. Paths were checked with `ls`, and every registered pointer was verified by locating the rule body and its enclosing heading in the canonical file — grepping a heading string only proves the heading exists, not that the rule lives under it. Mirror content was not verified and is not assumed to match the canonical file; runtime-managed caches are recorded as downstream consumer surfaces, not as replicas you may edit or quote.

## Rules

- Edit the canonical path only. Replicas and fallback files are read-only unless a migration or restore workflow explicitly says otherwise.
- Repo-tracked files are edited in this repo and then promoted into `~/.adk/release/`.
- Operator-managed runtime files under `~/.adk/release/config/` are edited in place. They are not currently git-tracked, so audit uses `agentdesk config audit`, targeted diffs, and structured backups instead of `git log`.
- Per-agent prompt files are NOT tracked in this repo (operator-private content). They live in the operator's Obsidian vault (`~/ObsidianVault/RemoteVault/adk-config/agents/`) and are pulled into `~/.adk/release/config/agents/` by `scripts/deploy-release.sh`.
- Legacy snapshots (`*.pre-*`, `*.bak`, `*.migrated`) are archive-only. They belong under `~/.adk/release/config/.backups/YYYY-MM-DD/`, not next to canonical files.
- Compatibility seams such as `role_map.json`, `bot_settings.json`, the root-level legacy `agentdesk.yaml`, and `_shared.md` aliases are not canonical write targets.
- Older docs with deprecated front matter are retained for history. Their config and policy lists are not write targets unless they point back here.

## Matrix

| Vector | Canonical file | Allowed replicas / consumers | Edit path | Audit command |
| --- | --- | --- | --- | --- |
| Runtime root layout | `~/.adk/release/` | Managed subtrees include `config/`, `policies/`, `skills/`, `bin/agentdesk`, and `data/`. | Change the root through `AGENTDESK_ROOT_DIR`, `agentdesk --init`, or deploy flow. Do not scatter runtime files into the repo. | `ls -la ~/.adk/release ~/.adk/release/config ~/.adk/release/policies` |
| Runtime baseline config | `~/.adk/release/config/agentdesk.yaml` | `~/.adk/release/agentdesk.yaml` is legacy fallback only. `agentdesk.example.yaml` documents shape, not live state. Runtime overrides in `kv_meta['runtime-config']` are a separate live-override surface. | Edit `~/.adk/release/config/agentdesk.yaml`. Do not edit the root fallback. | `./target/debug/agentdesk config audit --dry-run` |
| Discord bot bindings, agent roster, channel map | `~/.adk/release/config/agentdesk.yaml` (`discord:` and `agents[].channels`) | Legacy `~/.adk/release/config/role_map.json`, legacy `~/.adk/release/config/bot_settings.json`, and DB materialization can exist during migration, but `agentdesk.yaml` wins. | Edit `~/.adk/release/config/agentdesk.yaml` only. | `./target/debug/agentdesk config audit --dry-run` |
| Operational database routing and state | `~/.adk/release/config/agentdesk.yaml` (`database:` and `data:`) selects the database. Current verified config names Postgres `127.0.0.1:5432/agentdesk`; SQLite compatibility data lives at `~/.adk/release/data/agentdesk.sqlite`. | DB rows can mirror `agentdesk.yaml` agent/channel data during migration. File-canonical prompts, policies, MCP config, and memory paths must not be edited by changing DB rows. | Edit DB routing in `agentdesk.yaml`; edit operational records through AgentDesk APIs or dedicated migrations, not docs cleanup. | `rg -n "^(data\|database):" ~/.adk/release/config/agentdesk.yaml` |
| macOS release LaunchAgent | `~/Library/LaunchAgents/com.agentdesk.release.plist` | Generated by `agentdesk emit-launchd-plist` from `src/cli/init.rs`. `scripts/install.sh` and `scripts/deploy-release.sh` install/reload it. Env overlay: `~/.adk/release/config/launchd.env`. The generated plist sets the soft `NumberOfFiles` resource limit, clamped to the host hard limit, so tmux/provider sessions do not inherit launchd's low default soft limit without lowering the hard limit. | Edit `~/.adk/release/config/launchd.env` for operator env values. Persistent plist shape changes belong in the generator, under a code-scoped issue. | `plutil -lint ~/Library/LaunchAgents/com.agentdesk.release.plist && /usr/libexec/PlistBuddy -c 'Print :SoftResourceLimits:NumberOfFiles' ~/Library/LaunchAgents/com.agentdesk.release.plist` |
| Optional operator connectors | Connector env/config such as `AGENTDESK_OBSIDIAN_AGENTS_SRC` and `AGENTDESK_OBSIDIAN_SKILL_ROOT` | Doctor (`agentdesk doctor --json`) and dashboard settings (`GET /api/settings/operator-connectors`) read the same connector status model. Missing optional connectors are `skipped` or `invalid`, not core startup failures. | Edit the connector-specific env/config value. Do not add a global operator profile for P0 portability. | `agentdesk doctor --json --profile quick` and `curl -sf http://127.0.0.1:8791/api/settings/operator-connectors` |
| Per-agent prompt files | Obsidian `~/ObsidianVault/RemoteVault/adk-config/agents/<role>.prompt.md` | Release mirror `~/.adk/release/config/agents/<role>.prompt.md` is populated from Obsidian by `scripts/deploy-release.sh`. Repo does not carry these files (private content). Vault-side snapshots beside the canonical file (`<role>.prompt.md.pre-*`, `.bak`, `.migrated` — e.g. `project-agentdesk.prompt.md.pre-20260911-repo-discipline`) are archive-only and are never an edit target; the `config/.backups/` row below governs the release tree, not the vault. | Edit Obsidian prompt file, then redeploy. API path inspection runs at deploy staging (see below). | Inspect Obsidian git history (vault is synced separately) |
| Shared prompt | Obsidian `~/ObsidianVault/RemoteVault/adk-config/agents/_shared.prompt.md` | Release mirror `~/.adk/release/config/agents/_shared.prompt.md`; symlink aliases `~/.adk/release/config/agents/_shared.md` and `~/.adk/release/config/_shared.md`. Vault-side snapshots such as `_shared.prompt.md.pre-20260911-lanecap` are archive-only and are never an edit target. | Edit Obsidian `_shared.prompt.md`, then redeploy. Do not edit `_shared.md` aliases. | Inspect Obsidian vault |
| Policy hooks | Repo `policies/*.js` | Release mirror `~/.adk/release/policies/*.js`. | Edit repo policy file, then redeploy. | `git log -- policies/<name>.js` |
| Default pipeline | Repo `policies/default-pipeline.yaml` | Release mirror `~/.adk/release/policies/default-pipeline.yaml`; example pipelines under `policies/examples/` are references only. | Edit `policies/default-pipeline.yaml`. | `git log -- policies/default-pipeline.yaml` |
| Card timeout sweeps and the stage-failure moves they trigger | Repo `policies/timeouts/*.js` (11 files). The sweep logic and the failure move are both the JS; the duration thresholds come from runtime config keys such as `requested_timeout_min`, read via `agentdesk.config.get` in `policies/lib/timeouts-helpers.js`. | Release mirror `~/.adk/release/policies/timeouts/*.js`. The `timeouts:` block in `policies/default-pipeline.yaml` is NOT a second authority (#5718): no policy reads `config.timeouts`, `agentdesk.pipeline.getTimeout` has no callers, and its only reader is the preview-only `decide_timeout` reducer behind `agentdesk.timeouts.previewTimeoutDecision`, which emits the `[timeout_shadow]` comparison records for #3950 and never moves a card. A `stage_failure_policy:` block was removed from that manifest in #5718 because `PipelineConfig` never declared it. | Edit the relevant `policies/timeouts/*.js`, then redeploy. Do not encode timeout or failure-move policy in `default-pipeline.yaml`; unknown top-level keys there are now rejected at load. | `rg -n "getTimeoutInterval" policies/timeouts/ policies/lib/timeouts-helpers.js` |
| Policy SQL guard and capability manifest spec | [`docs/policy-sql-guard.md`](policy-sql-guard.md) | Runtime enforcement lives in `src/engine/sql_guard.rs`; future per-policy manifests should be repo files beside their policy. | Edit the spec before changing guard scope or adding a policy capability manifest. | `grep -rn "agentdesk.db." policies/` |
| Workspace agent contract (`CookingHeart/AGENTS.md`) | `~/CookingHeart/AGENTS.md` | None. This lives in the target workspace repo, not in AgentDesk. | Edit the workspace repo file directly. | `git -C ~/CookingHeart log -- AGENTS.md` |
| Claude home guidance | Optional symlink target of `~/.claude/CLAUDE.md` such as `~/ObsidianVault/RemoteVault/10_Claude/<host>/claude-home/CLAUDE.md` | `~/.claude/CLAUDE.md` is a symlink entry point only when the operator opts into a global Claude guide. | Edit the symlink target or use the CLAUDE relocation workflow. Do not replace the symlink with ad hoc content. | `readlink ~/.claude/CLAUDE.md` |
| AgentDesk repo CLAUDE guide | Repo `CLAUDE.md` | Repo-local agent entry point. It delegates canonical operational paths to this document. | Edit `CLAUDE.md` for repo-local agent instructions; edit this file for operational source mapping. | `git log -- CLAUDE.md docs/source-of-truth.md` |
| Workspace CLAUDE guide | `~/CookingHeart/CLAUDE.md` | Workspace-local copies or exports may exist elsewhere, but this file is the workspace contract. | Edit the workspace repo file directly. | `git -C ~/CookingHeart log -- CLAUDE.md` |
| Shared agent knowledge | `~/.adk/release/config/memories/shared-agent-knowledge/shared_knowledge.md` | Provider prompts and context builders consume it; it is not mirrored back into repo history. | Edit the runtime file or the managed memory workflow that owns it. Do not store turn history here. | `sed -n '1,80p' ~/.adk/release/config/memories/shared-agent-knowledge/shared_knowledge.md` |
| Shared agent memory (SAM) | `~/.adk/release/config/memories/shared-agent-memory/<agent>.json` | Runtime readers and memory sync tooling consume these JSON files. | Prefer tool-driven updates; manual edits are repair-only. | `ls ~/.adk/release/config/memories/shared-agent-memory/<agent>.json` |
| Long-term memory (LTM) | `~/.adk/release/config/memories/long-term/<agent>/` | Historical Obsidian long-term paths are legacy inputs; current managed path is under `config/memories/long-term/`. | Edit through the memory workflow or the canonical runtime files under this tree. | `find ~/.adk/release/config/memories/long-term/<agent> -maxdepth 2 -type f \| sort` |
| MCP server declarations | `~/.adk/release/config/agentdesk.yaml` (`mcp_servers:`) | Codex mirror: `~/.codex/config.toml` via `sync_codex_mcp_servers()`. OpenCode mirror: `~/.config/opencode/opencode.json` via `sync_opencode_mcp_servers()`. Claude consumer: `~/.claude/.mcp.json`. Provider config is a consumer surface, not the source. | Edit `~/.adk/release/config/agentdesk.yaml`, then restart or resync. Do not hand-edit provider mirrors unless repairing a broken sync. | `rg -n "mcp\|mcp_servers" ~/.adk/release/config/agentdesk.yaml ~/.codex/config.toml ~/.claude/.mcp.json ~/.config/opencode/opencode.json` |
| Skill manifest | Repo `skills/manifest.json` | Release mirror `~/.adk/release/skills/manifest.json`. Defines which `skills/<name>/` packages are exposed and to which providers. Per-skill content lives in Obsidian `~/ObsidianVault/RemoteVault/99_Skills/<name>/` and is fanned out by the `skill-sync` skill to Claude, Codex, Gemini, Qwen, and OpenCode runtime targets. | Edit `skills/manifest.json` for provider/global toggles; edit per-skill body in the Obsidian source then resync; redeploy AgentDesk to refresh release mirror. | `cat skills/manifest.json` (repo) and `ls ~/.adk/release/skills/` |
| Per-skill packages | Obsidian `~/ObsidianVault/RemoteVault/99_Skills/<skill>/SKILL.md` | Provider mirrors include `~/.claude/skills/<skill>/`, `~/.codex/skills/<skill>/`, OpenCode user/workspace skill roots, and the AgentDesk release mirror `~/.adk/release/skills/<skill>/` for skills exposed via `manifest.json`. The `skill-sync` skill manages the symlink/fanout. | Edit Obsidian source then run `skill-sync`. Do not hand-edit the provider mirrors. | `ls ~/ObsidianVault/RemoteVault/99_Skills/<skill>/` and `readlink ~/.claude/skills/<skill>` |
| Workspace CLAUDE.md per agent | `~/.adk/release/config/workspace-claude-md/<agent>.md` | Operator-managed file copied/symlinked into each agent's workspace as the agent-facing `CLAUDE.md`. Not git-tracked in this repo. | Edit the runtime file in place; it propagates on next workspace bootstrap. Do not hand-edit per-workspace `CLAUDE.md` copies. | `ls ~/.adk/release/config/workspace-claude-md/` |
| Workspace MEMORY.md per agent | `~/.adk/release/config/workspace-memory-md/<agent>.md` | Per-agent memory anchor consumed by the `memory-merge` workflow alongside SAM/SAK/LTM tiers. Workspace copies are derived; agent-managed memory entries live in `config/memories/` and the Obsidian Long-Term tree. | Edit the runtime file or use the `memory-write` / `memory-merge` skills. Avoid editing derived per-workspace copies. | `ls ~/.adk/release/config/workspace-memory-md/` |
| Operator routine scripts | Operator-private local `routines/**/*.js` in the release workspace, staged to `~/.adk/release/routines/` on deploy when present. Git ignores this tree; public repo history must not gain personal routine scripts. | The routine loader captures script metadata from the configured `routines.dir` plus optional `routines.additional_dirs`; `POST /api/routines/<id>/resume` validates metadata/checkpoint contracts before enabling a migrated routine. If local `routines/` is absent during deploy, the existing release runtime routines are retained. | Edit the operator-private local/runtime routine file. Do not commit routine JS to GitHub; repo changes should be limited to runtime engine code, API contracts, docs, or non-personal examples outside `routines/`. | `git ls-files routines` must be empty; `find routines -maxdepth 2 -type f` checks local-only scripts |
| `API_FRICTION:` marker contract | Repo [`docs/source-of-truth.md`](source-of-truth.md) and `/api/docs/api-friction-markers` | Runtime extraction/persistence lives in `src/services/discord/turn_bridge/` and `src/services/api_friction/`; events/rollups live in Postgres tables `api_friction_events` and `api_friction_issues`; Memento is an optional consumer. | Edit this document first when changing marker schema or operator contract; edit `src/services/api_friction/` only for runtime parser/storage behavior. Do not add ad hoc grep/cron collectors unless replacing the built-in collector under a dedicated migration. | `rg -n "API_FRICTION\|api_friction" docs src/services src/server/routes/docs.rs` |
| Lane-admission resource judgment (CPU idle, memory free %, memory pressure level, swapout rate, disk free) | Repo `scripts/lane_headroom.sh` — the thresholds AND the measurement method are the script, not prose. | Orchestrators call it before starting a lane and read only stdout line 1 (`HEADROOM: OK` / `HEADROOM: NO <reasons>`) and rc (0/1). Prompt/skill text (Obsidian `_shared.prompt.md` ★ section, `99_Skills/agentdesk-issue-pipeline/SKILL.md`) may reference the path but must not restate the numbers — frozen system prompts and compacted skill bodies are why the prose copy failed twice (#5438). `tests/test_lane_headroom_5438.sh` pins the output contract and the boundary values. The `scripts/_defaults.sh` deploy pre-flight (#4255) is a separate, deploy-scoped gate that fails OPEN; it is not a second authority for lane admission. | Edit `scripts/lane_headroom.sh` (thresholds live in its `LANE_HEADROOM_*` defaults, rationale in its header comment) and update the test in the same change. Do not copy thresholds into prompts, skills, or docs. | `bash scripts/lane_headroom.sh; echo "rc=$?"` and `bash tests/test_lane_headroom_5438.sh` |
| Orchestration lane caps and slot policy (how many lanes, and which lane kinds are capped at all) | Two Obsidian surfaces that must carry the same numbers: `~/ObsidianVault/RemoteVault/adk-config/agents/_shared.prompt.md` § `오케스트레이션 레인 유지 불변식` (always loaded, binding) and `~/ObsidianVault/RemoteVault/99_Skills/agentdesk-issue-pipeline/SKILL.md` (campaign long form, carries the measurements). The skill answers the question across three sections, not one: § `레인 & 슬롯` holds the concurrent cap and the floor, § `워크트리·브랜치 위생` holds the sub-limit on cargo-running lanes, and § `오케스트레이션 루프 — 이벤트 구동 리필` item 9 holds the rule that read-only lanes (review legs, design legs) are not subject to the cap at all. | Release prompt mirror `~/.adk/release/config/agents/_shared.prompt.md` is a derived copy refreshed by `scripts/deploy-release.sh`; it is a read surface only, and it is not automatically current — see the Edit path. `~/.codex/skills/agentdesk-issue-pipeline` is a symlink into the Obsidian vault, so it cannot drift. `~/.adk/release/skills/agentdesk-issue-pipeline/` is a runtime-managed cache: a consumer surface the runtime rebuilds from the vault, never an edit target and never a policy source. Three things share the name `skills/` and must not be conflated. (1) `scripts/deploy-release.sh` stages only the repo's own `skills/` packages (`rsync -a --delete "$REPO/skills/"`) and swaps the tree wholesale, so a deploy does delete this directory. (2) `ensure_runtime_layout()` in `src/runtime_layout/mod.rs` then puts it back: `migrate_legacy_skill_links` → `ensure_managed_skill_dir` in `src/runtime_layout/skill_sync.rs` walks the provider skill-link dirs (`~/.codex/skills/<skill>` among them — `collect_legacy_skill_target_dirs` covers claude/codex/gemini/qwen under the home dir and under every workspace) to their vault sources and re-copies each one under `~/.adk/release/skills/<skill>/`, refreshing a copy that has gone stale (#4256 fixed the copy-once bug here). `ensure_runtime_layout()` is the only path to that re-copy — `git grep -n 'ensure_runtime_layout(' -- src` lists 9 call sites in 7 files as of 2026-09-11 (`bootstrap`, `cli/client` ×2, `cli/dcserver`, `cli/direct`, `cli/init`, `cli/migrate/apply`, `services/onboarding` ×2). `sync_managed_skills()` — the call `/api/skills` and the Discord skill scan make — is not one of them: it lists whatever already sits under the cache (`discover_skill_dirs`) and re-deploys provider links from it, so it neither restores a cache a deploy has just deleted nor refreshes a stale one. (3) The Obsidian vault stays the canonical source. So the deletion is real but transient, and the directory is maintained — by the runtime, not by the repo or by `skills/manifest.json`, neither of which carries this skill. Read it as downstream of the vault; do not edit it and do not quote lane policy from it. Host headroom is NOT part of this row — that is `scripts/lane_headroom.sh` in the row above, and lane counts must never be inferred from it. | Edit both Obsidian sections in the same change and re-read them side by side, then propagate: redeploy so the release prompt mirror is rebuilt, and run `skill-sync` so the skill fanout is rebuilt. A vault-only edit leaves every runtime consumer on the superseded value — that is exactly how the release prompt mirror was still serving the old cap on 2026-09-11. A divergence between the two Obsidian surfaces is a live incident, not a doc nit: on 2026-09-11 they carried different caps and three review lanes were parked as "no slot" while the cap did not apply to that lane kind at all. Do not restate the numbers in repo docs or in this file. Both registered headings are inputs to the audit command in the next cell, not just prose for readers: write each as a plain ATX heading (`### Title`, or `##` for the prompt-mirror level) with no leading indentation, no trailing `#` closing sequence, no setext `----`/`====` underline instead, no zero-width space (U+200B) or non-breaking space (U+00A0) anywhere on the line, and no leading BOM; and keep the exact registered title string to exactly one occurrence in the whole file. The command's heading count presupposes this formatting, so a heading a markdown renderer would accept but that departs from this shape makes the count pass silently instead of catching the drift. | `c(){ s=$(awk -v h="$2" '$0 ~ "^[ \t]*\140\140\140"{q=!q;next} /^#/{p=index($0,h); if(p>1 && substr($0,p+length(h)) ~ /^[ \t]*$/) m++} !q&&/^#+[ \t]/{t=$0; sub(/^#+[ \t]+/,"",t); sub(/[ \t]+$/,"",t); if(t==h){n++;f=1} else f=0; next} f{b=b $0 "\n"} END{if(q\|\|m!=1\|\|n!=1) exit 1; printf "%s",b}' "$1") \|\| return 1; printf '%s\n' "$s" \| grep -oE '상한 ?[0-9]+\|[0-9]+ ?상한\|최소 ?[0-9]+' \| awk '{k=($0~/최소/)?"floor":"cap"; gsub(/[^0-9]/,""); print k"="$0}' \| sort -u \| tr '\n' ' '; }; a=$(c ~/ObsidianVault/RemoteVault/adk-config/agents/_shared.prompt.md '오케스트레이션 레인 유지 불변식'); ra=$?; b=$(c ~/ObsidianVault/RemoteVault/99_Skills/agentdesk-issue-pipeline/SKILL.md '레인 & 슬롯'); rb=$?; echo "[$a] vs [$b]"; [ "$ra$rb" = 00 ] && case $a in cap=*floor=*) [ "$a" = "$b" ];; *) false;; esac` — reads only the section that owns the numbers in each surface: the section opens at the heading whose text, after the leading `#` marks, equals the registered title exactly (so the `##`-level mirror heading and the `###`-level skill heading both match, while a look-alike such as `### 레인 & 슬롯 (구버전)` opens nothing and the real section is still the one read), and it is fence-aware, so a `#` comment inside a code block does not end the section. It labels every number by the word it is attached to (`상한` → `cap`, `최소` → `floor`), and compares the two labelled sets. It carries no literal number, does not care in which order the two values appear, and exits non-zero on one-sided drift, on the cap and the floor trading values (`cap=10 floor=16` vs `cap=16 floor=10`), and on the floor rule disappearing while some other `상한 10` keeps the digit in play — an unlabelled sorted set (`[10 16]`) passes both of those. Two independent counts gate the read and both must come out at exactly one: headings whose text equals the registered title (leading `#` marks stripped, surrounding spaces trimmed), and `#`-led lines that end in that exact title whatever separates the marks from it — an ASCII space, a tab, a full-width space, nothing at all — the second count taken with no regard for code fences, so a real heading pushed inside one still registers. An odd number of fence delimiters in the file is itself a failure. Anything other than one-and-one exits non-zero with no extracted numbers, and `c` returns that rc to the caller instead of losing it to the last command in a pipeline. Each of those guards exists because the round before it called the previous version closed: a decoy heading at another `#` depth beside a drifted real one, a duplicate of the real heading, a real heading hidden inside an unbalanced or misplaced fence, a real heading whose marks are followed by a full-width space. What it still does not do: a renamed section and a deleted one read identically (absent — fail closed, not silently passed); a `#`-led line inside a code block that happens to end in the registered title counts as a second candidate and fails the audit; and a decoy whose text differs from the registered title by a single character is invisible to both counts, so the title registered in this row, not the command, is what has to be kept honest. Assume further baits exist and re-derive the command against them rather than treating that list as complete. A `grep` for a fixed number passes when only one file matches; an order-sensitive `head -2` fails when either file is merely reordered or gains an unrelated number above the section. |
| Campaign (issue-pipeline) execution rules — landed-in-main judgment, build-token/`touch` ordering, mutation-lane commit proof, production-vs-test classification, recounting, scratchpad file ownership | Obsidian `~/ObsidianVault/RemoteVault/99_Skills/agentdesk-issue-pipeline/SKILL.md`. Per rule: the 5-step landed-in-main judgment whose step ⑸ is `git merge-base --is-ancestor <mergeCommit> origin/main` (a stacked PR can be MERGED with a matching `headRefOid` and still not be in main) → § `워크트리·브랜치 위생`; `touch` only after the build token is held and inside the token-holding command → § `검증 불변식` (NOT § `7.1 빌드 경합 제어 (build token)`, which covers the flock wrapper, sccache diagnosis, and stale `target/` cleanup and never mentions `touch`); mutation-lane blob-hash compare plus zero mutant markers in the pushed commit object, the ban on the "everything after the first `#[cfg(test)]` is test" heuristic, and recount-from-zero → § `검증 불변식`; owner-identified scratchpad filenames (`pr-body-<PR번호>.md`, `<이슈>-notes.md`) → § `오케스트레이션 루프 — 이벤트 구동 리필`. | Obsidian `adk-config/agents/project-agentdesk.prompt.md` § `repo_discipline` carries the always-on short form of the subset that also applies outside campaigns (build token / `touch` ordering, landed-in-main judgment, worktree and branch deletion). It is a second surface, not a second authority — the skill holds the measurements and the campaign-only rules. `~/.codex/skills/agentdesk-issue-pipeline` is a symlink into the vault and cannot drift. `~/.adk/release/skills/agentdesk-issue-pipeline/` is the runtime-managed cache described in the row above: the runtime re-copies it from the vault, so it sits downstream of this row's canonical file and is never an edit target or a quotable source. Because it is a cache it can lag the vault between refreshes, so a section you cannot find there has not necessarily been removed from the skill — resolve every question in this row against the Obsidian file, and treat a stale cache as a sync problem to investigate rather than as evidence about the rules. | Edit the Obsidian `SKILL.md` section, then run `skill-sync` to rebuild the fanout. If the short form changes too, update `project-agentdesk.prompt.md` in the same change and redeploy so the release prompt mirror follows. Register pointers here; do not copy rule bodies into repo docs or into this file. | `awk 'function chk(r,w){n++; if(seen[r]++){printf "DUP %s -> %s\n",r,h; b++; return} if(h=="### " w) printf "OK  %s -> %s\n",r,h; else {printf "BAD %s -> %s (want: ### %s)\n",r,h,w; b++}} $0 ~ "^[ \t]*\140\140\140"{q=!q;next} !q&&/^#+ /{h=$0} /mergeCommit/{chk("landed-in-main","워크트리·브랜치 위생 (2026-09-11 사용자 승인)")} /토큰 보유 명령/{chk("touch","검증 불변식")} /뮤턴트 마커/{chk("mutation","검증 불변식")} /이후는 전부 테스트/{chk("cfgtest-ban","검증 불변식")} /0에서 다시 세어라/{chk("recount","검증 불변식")} /소유자를 파일명으로/{chk("scratchpad","오케스트레이션 루프 — 이벤트 구동 리필 (2026-09-09 실측으로 규칙화)")} END{b+=(n!=6); exit(b>0)}' ~/ObsidianVault/RemoteVault/99_Skills/agentdesk-issue-pipeline/SKILL.md` — pairs every registered rule body with the exact heading line this row files it under, prints the heading each one actually sits under, and exits non-zero if any rule sits under any other heading (a look-alike such as `### 검증 불변식 (폐기됨)` is a different heading, not a match; so is a renamed or re-suffixed one), if any rule has gone missing, or if any marker phrase is hit more than once. Each marker phrase must occur exactly once in the skill: the command cannot tell a moved rule from a second mention, so a second hit prints `DUP` and fails, and a legitimate re-mention has to be reworded rather than allowed. A misfiled pointer therefore fails mechanically instead of waiting for someone to eyeball the output. Heading tracking is fence-aware: a `# ...` line inside a code block is shell comment text, not a heading, and an unguarded version attributes the next rule to it. |
| LaunchAgent operator env overlay | `~/.adk/release/config/launchd.env` | Loaded by the LaunchAgent generator (`agentdesk emit-launchd-plist`) and by `scripts/install.sh` / `scripts/deploy-release.sh`. Do not duplicate values into the plist directly. | Edit the runtime file. Reload via `launchctl bootout`/`bootstrap` or the deploy script. | `cat ~/.adk/release/config/launchd.env` |
| Memento MCP-backed memory | Memento MCP-managed store on disk (operator-private). Scope contract: [`docs/memory-scope.md`](memory-scope.md) (#1100). | `permanent` is for user identity, preferences, long-term decisions, and resolved procedures. `workspace`/`session` is for project/turn-local state. File-canonical content (prompts, runtime config, policies, skills, memory tiers listed in this matrix) MUST NOT be mirrored into Memento. When unsure, write `workspace`. Claude Code's provider-local auto-memory tree (`~/.claude/projects/<encoded-cwd>/memory/`) is not a Memento mirror and is not an AgentDesk memory surface — agents neither read nor write it. | Use the Memento MCP tool surface (`remember`, `amend`, `forget`, `memory_consolidate`). Avoid file-level edits to the MCP store. Promote `workspace` → `permanent` only via explicit `amend` after multi-session validation. `/api/memory/recall` and `/api/memory/forget` return an explicit unsupported/capability error only when runtime-active Memento is selected; they do not inspect or mutate the MCP-backed store. | `mcp__memento__memory_stats` and `mcp__memento__context` |
| PostgreSQL local memory fallback | PostgreSQL `local_memory` table managed by `migrations/postgres/0074_local_memory.sql` when Memento is unavailable or `ADK_FORCE_LOCAL_MEMORY=1`. | The table stores route-level fallback rows with `workspace` metadata; it does not implement Memento `permanent` scope, promotion, or amend semantics. File-canonical content still MUST NOT be mirrored here. | Use `/api/memory/{recall,remember,forget}` when the response reports `source: "local"` / local `detected_backend`, or direct SQL against `local_memory` for audits and repair. If runtime-active Memento is selected and you intentionally want local fallback rows, set `ADK_FORCE_LOCAL_MEMORY=1` before calling the route. Keep MCP-backed and fallback audit results separate. | `psql "$DATABASE_URL" -c "SELECT id, topic, kind, workspace, created_at FROM local_memory ORDER BY created_at DESC LIMIT 20;"` or `ADK_FORCE_LOCAL_MEMORY=1 curl -sf -X POST http://127.0.0.1:8791/api/memory/recall -H 'Content-Type: application/json' -d '{"limit":20}'` |
| Archived config snapshots | `~/.adk/release/config/.backups/YYYY-MM-DD/` | None. This is the only allowed home for `*.pre-*`, `*.bak`, and `*.migrated` snapshots. | Never edit in place. Restore or diff explicitly if needed. | `find ~/.adk/release/config/.backups -maxdepth 2 -type f \| sort` |

## Per-Vector Detail

The matrix above is the operational lookup. The per-vector notes below capture intent, common mistakes, and the smallest relevant invariant for each knowledge surface.

### Runtime baseline config (`config/agentdesk.yaml`)

The release-managed `agentdesk.yaml` is the entry point for almost every other vector — Discord bindings, agent roster, MCP server list, database routing, and pipeline selection all resolve through it. The repo carries `config/agentdesk.example.yaml` purely as a shape reference; it is not a write target. The legacy root-level `~/.adk/release/agentdesk.yaml` is a backwards-compat fallback for older binaries and must not be edited. Pitfall: editing the example file or the root fallback silently no-ops because the runtime resolves to `config/agentdesk.yaml` first; always re-run `agentdesk config audit --dry-run` after a change to confirm the live file reflects the intended state.

For a fresh portable operator, `python3 scripts/operator-init-portable.py --root ~/.adk/release` creates the canonical config path, prompt stubs, runtime directories, and `config/launchd.env.example`. It does not write secrets and does not overwrite existing files unless `--force` is supplied.

### Per-agent prompt files (`agents/<role>.prompt.md`)

Per-agent prompts are operator-private content and are deliberately excluded from this repo. The canonical files live in the Obsidian vault under `~/ObsidianVault/RemoteVault/adk-config/agents/`, and `scripts/deploy-release.sh` mirrors them into `~/.adk/release/config/agents/`. Pitfall: editing the release mirror directly survives until the next deploy and is then overwritten — always edit the Obsidian source. The flat `<role>.prompt.md` layout is current; the older `config/agents/<role>/IDENTITY.md` shape referenced in legacy docs is not in use and is not a valid edit target.

### Shared agent rules (`agents/_shared.prompt.md`)

Shared prompt instructions are appended to every per-agent prompt at composition time. Two compatibility symlinks (`config/agents/_shared.md` and `config/_shared.md`) point back at the canonical Obsidian-sourced file via the release mirror; never replace these aliases with copied text or edit the alias path. Pitfall: a missing alias is a deploy-time symptom — recreate the link from the canonical mirror, do not introduce a second canonical file.

The shared prompt also carries the in-prompt "정본 편집 경로 (Canonical Edit Path)" section (#1099) that points every agent back to this matrix and to the WIP commit discipline. When the matrix here changes, update that section in the Obsidian shared prompt as well so that agents see the new canonical path on their next turn.

### WIP commit discipline (`src/utils/wip_detect.rs`)

Uncommitted edits in an agent worktree are a known regression source: a fix that exists only on disk disappears as soon as the next dispatch wipes the worktree, and the cause becomes hard to recover. The runtime helper `check_wip_uncommitted_files()` in `src/utils/wip_detect.rs` reports staged/unstaged/untracked file counts for a given workspace, and the Discord turn-end warning facade (`src/services/discord/turn_end_wip_warning.rs`) emits a best-effort warning before completion surfaces when the active inflight row still points at dirty WIP. The shared prompt section above tells every agent to commit (or explicitly discard) WIP before ending a turn; the helper is the runtime-side check that the prompt rule is being honored. Pitfall: do not silence the warning by committing into the wrong worktree — verify the canonical path matrix first.

### Policies (`policies/*.js`) and default pipeline (`policies/default-pipeline.yaml`)

Policy hooks are repo-tracked JS files in `policies/` plus the YAML pipeline manifest that wires them together. Both promote into `~/.adk/release/policies/` via the deploy script. Policies are trusted automation: raw SQL, loopback HTTP, and allowlisted exec surfaces are available to policy code for operator-owned workflows, so the policy engine is not a sandbox for untrusted scripts. The current SQL guard is a state-transition safety rail, not a complete table denylist; its scope and the capability-manifest MVP are documented in [`docs/policy-sql-guard.md`](policy-sql-guard.md). Pitfall: examples under `policies/examples/` are reference snippets, not active configuration. Renaming a policy without updating `default-pipeline.yaml` will silently drop it from the active pipeline; verify with `git log -- policies/default-pipeline.yaml` after edits.

### CLAUDE.md hierarchy

Three distinct CLAUDE.md surfaces coexist and must not be conflated. The Claude home guidance under `~/.claude/CLAUDE.md` is a symlink into the Obsidian vault and is the global Claude Code instruction set. The repo-level `CLAUDE.md` carries AgentDesk-specific repo rules and delegates operational mappings to this document. Each downstream workspace (e.g. `~/CookingHeart/CLAUDE.md`) carries its own contract for that codebase. A fourth layer — per-agent `workspace-claude-md/<agent>.md` under `~/.adk/release/config/` — is bootstrapped into each agent's cwd as the workspace-local `CLAUDE.md`. Full load order, priority, and dedup rules are documented in [`docs/claude-md-load-order.md`](claude-md-load-order.md). Pitfall: do not copy content between layers — they have different audiences and different update cadences.

### MCP server declarations

MCP servers are declared once in `agentdesk.yaml` under `mcp_servers:` and fanned out by `sync_codex_mcp_servers()` into `~/.codex/config.toml`. Claude Code reads `~/.claude/.mcp.json`, which is also a derived consumer surface. Pitfall: hand-editing either provider mirror diverges them from the source and the next sync silently overwrites the change; always edit `agentdesk.yaml` and re-run the sync (or restart the daemon).

### Skills

Skills are two-layered. The repo carries a tiny `skills/` tree containing `manifest.json` plus the canonical AgentDesk-only skills (`memory-merge`, `memory-read`, `memory-write`); these promote into `~/.adk/release/skills/`. The full skill catalog lives in Obsidian under `~/ObsidianVault/RemoteVault/99_Skills/<skill>/SKILL.md` and is fanned out to `~/.claude/skills/` and `~/.codex/skills/` by the `skill-sync` skill. Pitfall: per-provider skill directories can also appear under the AgentDesk release tree for skills exposed via `manifest.json`; do not edit those mirrors directly.

### Memory tiers (SAM / SAK / LTM / workspace)

Shared agent knowledge (SAK) is a single `shared_knowledge.md`, shared agent memory (SAM) is per-agent JSON, and long-term memory (LTM) is a per-agent directory tree under `config/memories/long-term/<agent>/`. Workspace MEMORY anchors live separately under `config/workspace-memory-md/<agent>.md`. Pitfall: SAK and SAM are runtime-managed — manual edits should be repair-only, with content normally written through the `memory-write` / `memory-merge` skills. A second LTM tree exists in the Obsidian vault as legacy input; do not reintroduce it as canonical.

### API_FRICTION markers

`API_FRICTION:` is the canonical structured marker for API documentation gaps discovered during agent work. Emit it only when an agent had to infer a contract, trial-and-error an endpoint, or use a workaround because `/api/docs` was missing or misleading. The marker must be one physical line:

```text
API_FRICTION: {"endpoint":"PATCH /api/dispatches/{id}","friction_type":"missing-docs","summary":"dispatch completion docs omitted PATCH semantics","workaround":"read source and called PATCH manually","suggested_fix":"document status/result response fields in /api/docs/dispatches","docs_category":"dispatches"}
```

Required JSON fields are `endpoint`, `friction_type`, and `summary`. Optional fields are `workaround`, `suggested_fix`, `docs_category`, and `keywords`. Accepted aliases are `surface` for `endpoint`, `type` or `frictionType` for `friction_type`, `workaround_method` for `workaround`, `suggestedFix` for `suggested_fix`, and `docsCategory` for `docs_category`.

Runtime collection is already built in: the Discord turn bridge extracts valid marker lines, strips them from the delivered response, persists events to Postgres `api_friction_events`, optionally writes Memento topic `api-friction`, and the policy tick aggregates repeated fingerprints into `api_friction_issues` / GitHub issues. Invalid JSON markers remain visible in the response and are logged as parse errors so the agent can fix the marker.

Pitfall: do not add a parallel grep/cron collector for the same marker unless a dedicated migration replaces the runtime collector. Query the existing store first:

```sql
SELECT endpoint, friction_type, COUNT(*)
FROM api_friction_events
GROUP BY 1, 2
ORDER BY COUNT(*) DESC;
```

### LaunchAgent (`com.agentdesk.release.plist` + `launchd.env`)

The plist is generated, not hand-written; structural changes belong in the generator (`src/cli/init.rs`). Operator overrides go into `~/.adk/release/config/launchd.env`, which the generator and deploy scripts source. Pitfall: editing the plist directly drifts it from the generator and is reverted on the next `agentdesk emit-launchd-plist`. Use the env overlay for everything operator-tunable. The release plist also carries a generated soft `NumberOfFiles` limit clamped to the host hard limit; keep that in the generator so deploys preserve the FD budget without lowering a host-tuned hard limit.

### Workspace CLAUDE.md / MEMORY.md per agent

Per-agent workspace anchors at `config/workspace-claude-md/<agent>.md` and `config/workspace-memory-md/<agent>.md` are operator-managed runtime files (not repo-tracked). They are propagated into each agent's working directory at workspace bootstrap. Pitfall: edits made directly inside an agent worktree are lost on re-bootstrap — edit the runtime file under `~/.adk/release/config/`.

### Memento MCP-backed memory

MCP-backed Memento data is stored under the operator-private MCP server and is accessed through the `mcp__memento__*` tool surface. `/api/memory/*` is route-level dual-mode only where a bridge exists. `/api/memory/recall` and `/api/memory/forget` return `501` with `code: "memento_recall_unsupported"` / `code: "memento_forget_unsupported"` when runtime-active Memento is selected; they must not be treated as full MCP-backed Memento access until those bridges are implemented.

Audit MCP-backed memory with `mcp__memento__memory_stats` and `mcp__memento__context`. The permanent-vs-workspace scope contract is owned by [`docs/memory-scope.md`](memory-scope.md) (issue #1100): permanent scope is reserved for user identity, preferences, long-term decisions, and resolved procedures; everything else is `workspace`. Any content that has a canonical home in another row of this matrix (prompts, runtime config, policy files, skills, memory tiers) MUST NOT be mirrored into Memento. The Memento tool responses are authoritative for what they return — do not mirror Memento content back into repo files either.

### PostgreSQL local memory fallback

The fallback schema is owned by `migrations/postgres/0074_local_memory.sql`; `src/server/routes/memory_api.rs` only recalls, inserts, and deletes rows in the PostgreSQL `local_memory` table when Memento is unavailable or `ADK_FORCE_LOCAL_MEMORY=1` is set. This table has `workspace` metadata for route-level filtering, but it does not implement Memento `permanent` scope, promotion, or amend semantics.

Audit fallback memory separately from MCP-backed memory. Query the table directly with `psql "$DATABASE_URL" -c "SELECT id, topic, kind, workspace, created_at FROM local_memory ORDER BY created_at DESC LIMIT 20;"` or call `ADK_FORCE_LOCAL_MEMORY=1 curl -sf -X POST http://127.0.0.1:8791/api/memory/recall -H 'Content-Type: application/json' -d '{"limit":20}'` and verify the response reports `source: "local"` while recording `detected_backend`. Because MCP recall/forget bridges are still incomplete, fallback recall/forget only covers the PostgreSQL table, not the MCP-backed Memento store.

### Archived config snapshots

Backups and migration residues (`*.pre-*`, `*.bak`, `*.migrated`, plus the one-shot `config.backup-v1/` directory) are archive material only. The single allowed home is `~/.adk/release/config/.backups/YYYY-MM-DD/`. Pitfall: leaving snapshot files next to canonical config makes audit tooling noisier and risks accidental restore; move them with `scripts/archive-config-backups.sh` when config cleanup is in scope.

## Notes

- `~/.adk/release/config.backup-v1/` is a separate one-time migration archive. Leave it untouched unless a dedicated cleanup issue handles it.
- The settings precedence contract for YAML vs `kv_meta` runtime overrides lives in [docs/adr-settings-precedence.md](adr-settings-precedence.md).
- If `agentdesk.yaml` and a legacy file disagree, follow the current code rule: `agentdesk.yaml` wins and legacy files are migration inputs or stale snapshots.
- Current verified prompt files are flat `<role>.prompt.md` files. Do not document or edit `config/agents/<role>/IDENTITY.md` as canonical until those files actually exist and a dedicated migration changes the prompt layout.
- Memento scope (permanent vs workspace) is owned by [`docs/memory-scope.md`](memory-scope.md). Config-domain split (runtime-config / dashboard / bot-settings) is owned by [`docs/config-domains.md`](config-domains.md).

## Symlink Rules

- Only documented compatibility aliases are allowed: `~/.adk/release/config/agents/_shared.md -> _shared.prompt.md` and `~/.adk/release/config/_shared.md -> agents/_shared.prompt.md`.
- The symlink target is canonical. Never edit `_shared.md` aliases directly, and do not replace an alias with copied prompt text.
- If an alias is missing, recreate the link from the canonical target during deploy/repair. Do not use symlinks to hide backup snapshots or introduce alternate config names.

## Deprecated References

- `docs/architecture.md` is retained for historical architecture context and is marked deprecated. Its migration-era `role_map.json` references and old policy filenames such as `review-policy.js`, `timeout-policy.js`, and `reward-policy.js` are not canonical write targets.
- `~/.adk/release/config/bot_settings.json.migrated` was archived into `~/.adk/release/config/.backups/2026-04-24/` by `scripts/archive-config-backups.sh` on 2026-04-24 (#1098). The canonical surface is `agentdesk.yaml`; do not reintroduce legacy snapshot files beside canonical config.

## Code-Side Companion

This document answers "which file do I edit?" for runtime/config/operator
surfaces. The code-side companion that answers "which Rust module do I edit?"
for in-flight migrations and known-legacy paths lives under
[`docs/agent-maintenance/`](agent-maintenance/index.md) (#1279):

- [`agent-maintenance/change-surfaces.md`](agent-maintenance/change-surfaces.md) — canonical modules, giant-file hard-gate source of truth, `do_not_edit_without_migration_plan` list.
- [`agent-maintenance/discord-outbound-migration.md`](agent-maintenance/discord-outbound-migration.md) — per-callsite coverage map (#1006, #1175, #1280).
- [`agent-maintenance/known-legacy.md`](agent-maintenance/known-legacy.md) — intentionally-legacy modules with cleanup-owner issue numbers.

Prompt API path inspection (`scripts/check_prompt_api_routes.py`) reads flat staged `*.prompt.md` files and this checkout’s endpoint inventory; it skips symlinks.
Only explicit loopback URLs with literal ports in prose or sh/bash/shell fences are checked; comments, blockquotes, other fences and relative paths are excluded. A complete URL token counts no matter what precedes it; only a path continuing past its own closing quote (`/`, `$`, `{`, a quote, or `+`) reads as a shell join.
Unknown paths (exit 1) and unavailable inspection (exit 2) warn without blocking deploy; zero candidates means `no_applicable_urls`. This is source path lint, not runtime/method validation; doctor, strict mode and mirror refresh verification remain separate.
