What changed:
- Added `tabIndex={-1}` to the "Next Sprite" (▲) and "Previous Sprite" (▼) buttons in `AgentFormModal.tsx` to remove them from the tab sequence.
- Simplified the `aria-valuetext` on the spinbutton in `AgentFormModal.tsx` to announce "Sprite X" instead of redundantly announcing "Selected sprite X".
- Updated the terminology for the `EmojiPicker` in `AgentFormModal.tsx` from "Emoji" (이모지) to "Icon" (아이콘) to align with `DepartmentFormModal.tsx`.

Why:
- Removing redundant tab stops adheres to W3C ARIA Authoring Practices Guide for `spinbutton` widgets (which manage their own up/down interaction via arrow keys).
- Simplifying the `aria-valuetext` avoids screen readers redundantly reading "Selected", providing a cleaner audio experience.
- Harmonizing "Emoji" to "Icon" matches other form labels, improving consistency across the dashboard.
- This recreates the valuable parts of the abandoned/contaminated branches (#196 and #202) cleanly.

WorkFingerprint:
- Agent: Accessor
- Category Boundary: dashboard/src/components/agent-manager/AgentFormModal.tsx
- Invariant Protected: Keyboard navigation boundaries and localized accessibility labels.
- Public API Impact: None.
- Docs Impact: None.
- Verification Plan: Run `npm run test` and `./scripts/verify-dashboard.sh`.
- Related PRs/Issues: Resolves #196 and #202 cleanly without stale branch pollution.

Duplicate/Overlap Check:
- Verified via `git branch -a` and examining recent Accessor commits (`origin/jules/accessor/modal-selector-a11y-*`). The other branches were found to be either empty no-change reports yielding to others, or massively contaminated broad branches (`2800+ files changed`). I am committing *only* the specific accessibility fix cleanly on top of `main`.

Verifications:
- `npm run test` (dashboard components) - Passed.
- `./scripts/verify-dashboard.sh` - Passed.

Skipped Checks:
- None.

Risk:
- Very Low. Modifies standard ARIA attributes (`tabIndex`, `aria-valuetext`) and text nodes without changing application logic or runtime state.

Rollback Notes:
- Revert the commit to restore previous tab stops and labels. No DB migration required.
