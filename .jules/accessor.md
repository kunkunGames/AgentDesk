## Accessibility Patterns

- **Color Pickers:** When using `map` to render color swatches as `<button>` elements, always provide an `aria-label` identifying the color and use `aria-pressed` to indicate selection state.
