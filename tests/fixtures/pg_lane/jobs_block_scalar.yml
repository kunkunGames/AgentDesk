on:
  push:
jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - run: |
          cat <<'EOF'
          setup
          EOF
  pg_lane:
    runs-on: ubuntu-latest
    steps:
      - run: ./scripts/ci/postgres-service.sh start
      - run: cargo test postgres_
