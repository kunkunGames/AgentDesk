What changed:
Superseded by #2086; producing empty commit report instead of salvaging stale branch.

Why:
The PR was marked as superseded by #2086, which includes this diagnostic update. Creating an empty commit to report no-change and abandon the task to avoid duplicate PRs.

WorkFingerprint:
- Agent name: Doctor
- Category boundary: `src/cli/doctor/**`
- Primary files: `src/cli/doctor/health.rs`
- Invariant protected: The live doctor `next_step` instructions shouldn't refer to SQLite.
- Public API impact: None (no-change).
- Docs impact: None (no-change).

Overlap Check:
The PR comment indicates it is superseded by #2086. Following rules for "If Doctor finds an overlapping open PR or the safe change is unclear, it must stop and produce an empty commit no-change report instead of creating another PR."

Verifications:
- `git diff --check`
- `cargo check --all-targets`

Skipped checks:
- None

Risk: None
Rollback notes: None
