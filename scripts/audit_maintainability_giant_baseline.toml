# Giant-file re-inflation ratchet baseline (#3028).
#
# Each entry freezes a production giant at its current production LoC (test code
# excluded, consistent with the `giant_files` check / #3036). The
# `giant_file_ratchet` audit check fails `--check` when a file's production LoC
# exceeds its frozen value, so a decomposed module cannot silently re-inflate
# (tmux_watcher.rs had regrown 7160 -> 9608 with no upper bound).
#
# Workflow:
#   * As a file shrinks, LOWER its number here to lock in the win.
#   * Growing a giant requires RAISING its number — a deliberate, reviewable
#     admission in the diff that the giant got bigger. Prefer splitting instead.
#
# Scope (this batch): the M2.x turn-lifecycle decomposition surface
# (src/services/discord/** and src/services/turn_*), where re-inflation was
# reported. Other production giants remain covered by the `giant_files`
# documentation gate; extend this table to ratchet them too.

[giant_file_ratchet]
# #3038 execute_streaming S3: lowered claude.rs 3989 -> 2950 by moving the
# TUI warm/follow-up hosting helpers into claude_tui/hosting/ child modules.
# #3262 turn-lock acquisition/state/tests intentionally remain in claude.rs root.
"src/services/claude.rs" = 2963
# #3034 batch-8: the dead_code lint went live for the discord tree; the two
# test-only `#[allow(dead_code)]` attributes on
# `watcher_inflight_is_external_input_for_session` / the lease alias add +2 and
# the dead `last_edit_text` assignment removal drops -1 (net +1). Rebased onto
# #3262/#3272, which extracted the inline completed-panel backfill block into
# `adk_session::backfill_completed_panel_usage_and_maybe_inject_compact` (−16
# prod lines), so the frozen true production LoC settles at 9583. KEPT surfaces.
# #3296: the aborted-anchor reconcile chokepoint — one guarded block recording
# the `tui_direct_abort_marker::record_commit_tombstone` commit evidence and
# calling `drain_on_terminal_commit`, sited BEFORE `clear_inflight_state`
# (codex r2: tombstone → drain → clear, so a racing sweep that observes "no
# live row" always sees the commit evidence) and passing the committed turn's
# identity (codex r1 positive correlation; the marker logic lives in the
# non-giant `tui_direct_abort_marker/` directory module — decomposed by #3303
# into mod.rs + store.rs + deferred_claim.rs) — offset in the same change by
# compressing the #3016-S3 finalize/TOCTOU and #1670/#1708 decoupling comment
# blocks (issue references and rationale preserved), so the file lands at
# 9580 under the unchanged frozen baseline (no raise).
# #3038 tmux_watcher S1: lowered 9583 -> 8122 by moving top-level decision
# clusters A/B/C/E/F/I/J/K into tmux_watcher/ child modules. The relay loop,
# D/H clusters, and relay path remain in the root.
# #3351: +101 for placeholder-reclaim wiring in the relay loop (tick/terminal/
# fresh-idle reclaim sites + r21 durable-record drops); the helper bodies live
# in tmux_watcher/placeholder_reclaim.rs. tmux_watcher S2 re-lowers this root.
# #3479 Phase-1 rank-1: lowered 8223 -> 7485 by extracting the supervisor
# relay-forward + session-bound terminal-ACK cluster verbatim (pure move, zero
# logic change) into two cohesive tmux_watcher/ child modules — supervisor_relay.rs
# (forward half + shared SessionBoundRelayAckTarget/SupervisorRelayForward types +
# watcher_terminal_commit_fence/terminal_event_consumed_offset) and
# session_bound_ack.rs (SessionBoundRelayAckOutcome fold, per-sequence ACK snapshot
# resolvers, watcher-direct-send gate, WatcherTerminalResendAction + in-flight-sink
# marker gate, RelaySlotGuard, ACK delivery wait); moved unit tests live in sibling
# supervisor_relay_tests.rs / session_bound_ack_tests.rs. Split into two child files
# only to keep each within the tmux_watcher/** 700-line namespace cap.
# #3479 Phase-1 rank-2: lowered 7485 -> 7241 by extracting two more cohesive PURE
# clusters verbatim (pure move, zero logic change) into tmux_watcher/ child modules
# — utf8_chunk_decoder.rs (Utf8ChunkDecoder + DecodedUtf8Chunk streaming decoder)
# and terminal_readiness.rs (synchronous terminal-readiness / inflight-classification
# predicates + the pure buffer/message-id reconcilers). The async, shared-touching
# commit_watcher_direct_terminal_session_idle (which sits between the two readiness
# clusters in the root) deliberately STAYS here; moved unit tests live in sibling
# utf8_chunk_decoder_tests.rs / terminal_readiness_tests.rs (excluded from the
# production splitter).
# #3479 SPC-shadow removal: lowered 6939 -> 6922 (deleted the dead StatusPanelController
# shadow-parity calls — 2x shadow_adopt_liveness_reacquired_panel + assert_watcher_create_parity;
# legacy reacquire/create/edit IO unchanged).
# #3558 raised 6923 -> 6925: both `persist_watcher_stream_progress` streaming call
# sites now thread the captured `turn_identity_for_panel.as_ref()` into the new
# single-flock RMW helper so a late-frame fresh row B is rejected (+2 lines).
# #3558 (codex review follow-up) raised 6925 -> 6935: the two remaining
# session-bound-relay-success sites that still did an unlocked
# `load_inflight_state` -> mutate -> `save_inflight_state` (re-writing a stale
# `last_offset`/`response_sent_offset`) now route through the new
# `persist_watcher_relay_watermark_locked` single-flock RMW helper, gated on the
# captured `inflight_identity_before_relay` (+10 lines of identity-guarded calls
# and explanatory comments).
# #3579 raised 6935 -> 6946: the `session_bound_ack_outcome` INIT switched from
# the `MissingTarget` failure sentinel to the new watcher-owned `NotAttempted`
# non-attempt sentinel, with a load-bearing explanatory comment block at the init
# site documenting why the skipped-ack-wait benign case is now distinct from the
# real `wait_for_session_bound_relay_delivery_ack` target-none failure. NO new
# direct send / behavior change — provenance/aggregation classification only.
# #3610 PR-1d: watcher legacy long-chunk arm anchor instrumentation (+M4 reasoning comments)
# #3646 raised 6970 -> 7059 (+89 prod LoC): OBSERVATION-ONLY relay-owner split +
# three terminal lifecycle event emits (terminal_body_commit / terminal_ui_transition /
# inflight_clear + a NON-FATAL invariant signal). NO relay/cleanup behaviour change —
# the flight-recorder owner field was split (one tracing field) and three thin
# pass-through emit call sites were added; all orchestration/payload/invariant logic
# lives in the sibling non-hot relay_owner_observability.rs. The emits only gate the
# EMIT (never the cleanup); the invariant is error-event + debug_assert (no operational
# panic). Resolves the #3607 None-ledger vs Watcher-finalize ambiguity.
# #3646 codex review #3678: 7059 -> 7065 (+6 prod LoC): preserve the legacy
# `relay_owner_kind` tracing field as a back-compat alias next to the new
# `inflight_relay_owner` (owner string lifted into a `let`). Tracing fields only.
"src/services/discord/tmux_watcher.rs" = 7065
# #3038 tmux_watcher S1 child modules, frozen at landed production LoC.
"src/services/discord/tmux_watcher/commit_decisions.rs" = 140
"src/services/discord/tmux_watcher/completion_gate.rs" = 275
"src/services/discord/tmux_watcher/liveness.rs" = 301
"src/services/discord/tmux_watcher/panel_decisions.rs" = 372
"src/services/discord/tmux_watcher/placeholder_reclaim.rs" = 106
"src/services/discord/tmux_watcher/prompt_observe.rs" = 109
"src/services/discord/tmux_watcher/turn_identity.rs" = 327
# #3479 Phase-1 rank-1 child modules (supervisor relay-forward + session-bound
# terminal-ACK cluster), frozen at landed production LoC. Both sit under the
# tmux_watcher/** 700-line namespace cap; their moved unit tests live in sibling
# *_tests.rs files (excluded from the production splitter).
"src/services/discord/tmux_watcher/supervisor_relay.rs" = 393
# #3579 raised 404 -> 427: added the `NotAttempted` enum variant + its fold arm
# and a doc block distinguishing the watcher-owned non-attempt from the real
# `MissingTarget` target-none failure (behavior-preserving; resend decision folds
# to `DeliveryOutcome::Unknown` identically). Stays well under the 700-line cap;
# the new sentinel-distinction unit tests live in sibling session_bound_ack_tests.rs.
"src/services/discord/tmux_watcher/session_bound_ack.rs" = 427
# #3479 Phase-1 rank-2 child modules (streaming UTF-8 chunk decoder + terminal-
# readiness predicates / pure buffer reconcilers), frozen at landed production LoC.
# Both sit under the tmux_watcher/** 700-line namespace cap; their moved unit tests
# live in sibling *_tests.rs files (excluded from the production splitter).
"src/services/discord/tmux_watcher/utf8_chunk_decoder.rs" = 88
"src/services/discord/tmux_watcher/terminal_readiness.rs" = 214
# #3089 A4 watcher short-replace controller cutover sibling, frozen at landed prod LoC.
# A4 r2 (codex r1 [High]): +118 for the `apply_watcher_short_replace_result` split
# + the `DeliveredFallback` write-back arm mirroring the legacy fallback cleanup.
"src/services/discord/tmux_watcher/terminal_send.rs" = 584
# Frozen at its true production LoC once #3028 fixed the prod/test splitter
# (lifetime/char-literal brace miscount): the first inline `#[cfg(test)] mod`
# block was over-extended to EOF, so `spawn_turn_bridge` and ~9000 lines of the
# turn-lifecycle surface were mislabeled test. This is the actual #3028 guard.
# #3038 turn_bridge S1: lowered 8417 -> 6778 by moving the helper-zone
# status-panel, voice-completion, headless-delivery, memory-observation,
# analytics, and confirmed-end clusters out of the root while leaving the
# `spawn_turn_bridge` async body byte-identical.
# #3479 Phase-1 rank-3: lowered 6811 -> 6702 (-109) by moving the pure,
# unit-tested cancel/finalize decision helpers (classify_turn_finished_dispatch_kind,
# is_done_setting_terminal_frame, should_finalize_cancel_after_recv,
# should_suppress_headless_delivery_for_cancel, should_record_final_turn_transcript,
# resolve_bridge_owner_channel) + their tests into the new sibling
# `turn_bridge/cancel_finalize_policy.rs` (re-exported, so call sites stay
# byte-identical). Locks in the shrink win (ratchet down only).
# #3479 Phase-1 rank-4: lowered 6702 -> 6535 (-167) by moving the pure
# streaming-edit-text + pre-submission/transport TUI prompt-error classifiers
# (build_turn_bridge_streaming_edit_text, bridge_pre_submission_tui_prompt_error,
# bridge_tui_transport_error_should_skip_quiescence) into the new sibling
# `turn_bridge/streaming_edit_text.rs`, and the watcher-orphan spinner-cleanup
# decision + retry-spawn helpers (should_delete_bridge_created_watcher_orphan_response,
# should_retry_watcher_orphan_spinner_cleanup, record_watcher_orphan_spinner_cleanup,
# spawn_watcher_orphan_spinner_cleanup_retry) into
# `turn_bridge/watcher_orphan_cleanup.rs` (both re-exported, call sites
# byte-identical). Locks in the shrink win (ratchet down only).
# #3560: +9 (6189 -> 6198) for the footer-mode migration reconcile — the
# migrate_separate_status_panel_to_footer call + comment at the footer-mode entry
# (helper body itself extracted to turn_bridge/status_panel.rs, net decomposition).
# #3607: +20 raw / +20 prod for the TimedOut+committed terminal-UI obligation
# hook. The durable sidecar store + isolated sweeper live in
# terminal_ui_obligation.rs; this keeps the hot-file cost to the call-site hook.
# #3859: 6241 -> 6248 (+7 production LoC). The InflightCleanupGuard gains a
# token_hash field so its abnormal-exit cleanup routes through
# request_inflight_abandon_if_matches (durable abandon-request enqueue + row
# delete) instead of a bare clear — finalizing a failure-path-stranded
# "처리 중" placeholder without keeping the inflight row alive. Bugfix-only.
"src/services/discord/turn_bridge/mod.rs" = 6248 # #3607 terminal-UI obligation hook
# #3038 turn_bridge S1 child modules, frozen at landed production LoC.
"src/services/discord/turn_bridge/headless_delivery.rs" = 415
"src/services/discord/turn_bridge/status_panel.rs" = 437
"src/services/discord/turn_bridge/voice_completion.rs" = 385
# #3038 S1: lowered 5188 -> 4987 (-201) as `QueuedPlaceholderState` (cluster C:
# 3 fields + 9 inherent methods) moved to the `shared_state` sibling module.
# Locks in the shrink win (ratchet down only).
# #3293: the +13 closed-retry rewires (`mailbox_peek` + routing the recovery
# kickoff / intervention enqueue through `*_with_closed_retry`) are offset by
# queue-exit comment dedup in the same root, so the frozen baseline stays at
# 4987 (no raise; #3028 ratchet).
# #3038 S2: lowered 4987 -> 4980 (-7) as the cluster-D field block (8 fields)
# moved into `shared_state::SessionOverrideState`. Locks in the shrink win.
# #3038 S3: lowered 4980 -> 4965 (-15) as the cluster-E field block (13
# restart-lifecycle fields) moved into `shared_state::RestartLifecycle`.
# Locks in the shrink win (ratchet down only).
# #3089 S0 (updated #3560): +5 for the single_message_panel wrapper/log trigger
# in mod.rs; parser/cache body lives in `single_message_panel.rs`. As of #3560
# the flag is default-ON (opt-out via AGENTDESK_SINGLE_MESSAGE_PANEL=0|false).
# #3038 S4: lowered 4970 -> 4967 (-3) as the cluster-F field block (5
# live-placeholder/status-panel fields) moved into `shared_state::PlaceholderState`.
# Locks in the shrink win (ratchet down only).
# #3038 S5: lowered 4967 -> 4944 (-23) as the cluster-G runtime HTTP cache
# fields plus the `serenity_http_or_token_fallback` accessor moved into
# `shared_state::RuntimeHttpCache`. Locks in the shrink win (ratchet down only).
# #3479 item-2 (behavior-preserving extraction): the catch-up subsystem
# (restart-gap message recovery — classify/scan/enqueue + its tests) moved
# verbatim to the capped sibling `catch_up.rs`, lowering the frozen baseline
# 4979 -> 4243 (-736). Locks in the shrink win (#3028 ratchet down only).
# #3479: the dispatch-policy cluster (monitor-auto-turn origin marker codec,
# stale-dispatch turn gating + its tests, allowed-turn-sender / bot-user-id
# resolvers, phase2 recovery predicate) moved verbatim to the capped sibling
# `dispatch_policy.rs`, lowering the frozen baseline 4243 -> 4074 (-169).
# #3557 (A): per-turn hard-ceiling backstop helpers (turn_hard_ceiling_timeout,
# codex_turn_hard_ceiling_timeout, turn_hard_ceiling_deadline_ms,
# clamp_auto_extend_deadline_ms) + their unit tests. +55 prod LoC. 4056 -> 4111.
# #3856/#3854 merge train: prompt-relay and inflight helper decomposition changed
# the module inventory surface while shrinking `tui_prompt_relay.rs` below the
# giant threshold; refresh the frozen parent-module count so script checks reflect
# current main.
# #3864 (P1 data-loss): swap the now-dead `mailbox_replace_queue` wrapper for the
# new in-actor `mailbox_merge_restored_queue_items` wrapper (restore now merges
# inside the mailbox actor instead of a racy out-of-actor snapshot->replace).
# Net +6 prod LoC. 4147 -> 4153.
# #3859: 4153 -> 4154 (+1 production LoC) — `mod abandon_request_store;`
# declaration for the new durable abandon-request store (the failure-path
# placeholder finalize handoff). Bugfix-only.
"src/services/discord/mod.rs" = 4154
# #3305: the local-only pass-through lifecycle-skip (kind hoist + note-only return
# branch + idle-loop skip) is offset by comment dedup in the same root, lowering the
# frozen baseline 5438 -> 5429 (-9). Locks in the shrink win (#3028 ratchet).
# windows-tmux cfg fix (#3461): +5 prod LoC for the `#[cfg(not(unix))]`
# committed-frontier carry-forward fallback (cross-platform compile). 5429 -> 5434.
# #3479 rank-5 (behavior-preserving extraction): the pure injected-prompt
# classification/formatting cluster moved verbatim to the capped sibling
# `tui_prompt_relay/injected_prompt_policy.rs` (318 prod LoC). Lowers the frozen
# baseline 5434 -> 5142 (-292). Locks in the shrink win (zero logic change).
# #3479 rank-10 (behavior-preserving extraction): the pure transcript/rollout
# prompt scanners moved verbatim to `tui_prompt_relay/idle_transcript_scan.rs`
# (~278 LoC) and the Discord-IO/SharedData-coupled Claude TUI binding
# rehydration + dead/orphaned-session eviction pass to
# `tui_prompt_relay/rehydration.rs` (~296 LoC), both reached via re-import so the
# call sites stay byte-identical. Lowers the frozen baseline 5142 -> 4630 (-512).
# Locks in the shrink win (zero logic change).
# #3479 item-2 (behavior-preserving extraction): the live-relay TUI-direct prompt
# anchor COMPLETION lifecycle (`⏳ → ✅`) cluster — the visibility gate, the
# deferred `⏳`-completion drain decision, and the reaction-swap completers —
# moved verbatim to `tui_prompt_relay/anchor_completion.rs` (213 prod LoC, below
# the giant threshold), reached via re-import so the call sites stay
# byte-identical. Lowers the frozen baseline 4630 -> 4458 (-172). Locks in the
# shrink win (zero logic change).
# #3479 launch-script (behavior-preserving extraction): the Claude TUI
# launch-*script* parsing cluster (`ClaudeTuiLaunchInfo`, `parse_claude_tui_launch_script`
# + `_content`, and the `shell_words_from_line` single-quote splitter) moved
# verbatim to `tui_prompt_relay/launch_script.rs` (107 prod LoC, below the giant
# threshold), reached via re-import so the call sites stay byte-identical. Lowers
# the frozen baseline 4458 -> 4376 (-82). Locks in the shrink win (zero logic change).
# #3833 (behavior-preserving decomposition): external-input ownership,
# synthetic-start, and Claude idle-tail/runtime/bridge clusters moved into
# `tui_prompt_relay/` child modules. The root drops below the prod-giant
# threshold, but keep a sub-threshold frozen ceiling so re-inflation trips this
# gate if it crosses 1000 prod LoC again.
"src/services/discord/tui_prompt_relay.rs" = 999
# #3038: lowered 4728 -> 4657 (-71) as `SensitivityState` (definition + impl)
# moved to the `voice_sensitivity` sibling module. Locks in the shrink win.
# #3038 VoiceBargeInRuntime S1: lowered 4657 -> 4350 (-307) as the STT
# method cluster moved to `voice_barge_in/stt.rs` (314 prod LoC, below the
# giant threshold). Locks in the shrink win.
# #3038 VoiceBargeInRuntime S2: lowered 4350 -> 3932 (-418) as the progress
# playback method cluster moved to `voice_barge_in/progress_playback.rs` (423
# prod LoC, below the giant threshold). Locks in the shrink win.
# #3038 VoiceBargeInRuntime S3: lowered 3932 -> 3710 (-222) as the final-result
# playback method cluster moved to `voice_barge_in/final_result_playback.rs`
# (230 prod LoC, below the giant threshold). Locks in the shrink win.
# #3038 VoiceBargeInRuntime S4: lowered 3710 -> 3332 (-378) as the routing
# resolution method cluster moved to `voice_barge_in/routing.rs` (383 prod LoC,
# below the giant threshold). Locks in the shrink win.
# #3038 VoiceBargeInRuntime S5: lowered 3332 -> 3217 (-115) as the live-cut
# playback method cluster moved to `voice_barge_in/live_cut_playback.rs` (120
# prod LoC, below the giant threshold). Locks in the shrink win.
# #3038 VoiceBargeInRuntime S6: lowered 3217 -> 3136 (-81) as the TTS pipeline
# method cluster moved to `voice_barge_in/tts_pipeline.rs` (86 prod LoC, below
# the giant threshold). Locks in the shrink win.
# #3038 VoiceBargeInRuntime S7: lowered 3136 -> 3044 (-92) as the agent-voice
# routing helper block folded into `voice_barge_in/routing.rs` (383 -> 484 prod
# LoC, below the giant threshold). Locks in the shrink win.
# #3038 VoiceBargeInRuntime S8: lowered 3044 -> 2823 (-221) as the foreground
# decision/parser cluster moved to `voice_barge_in/foreground_decision.rs` (214
# prod LoC, below the giant threshold). Locks in the shrink win.
"src/services/discord/voice_barge_in.rs" = 2823
# #3248: +60 for the gap-1 deploy-survivable-relay fix —
# `reseed_watcher_owned_finalizer_ledger` + two guarded reattach call-sites that
# re-register the watcher-owned turn in the post-restart finalizer ledger (a P1
# live-ops fix; the helper is the minimum surface, kept in-file beside its sole
# caller `reregister_active_turn_from_inflight`).
# #3034 batch-8: lowered 4094 -> 4090 (dead-code removal: dropped `assistant_turns`
# construction sites).
# #3479 r8: lowered 4090 -> 3718 (behavior-preserving extraction of the pure
# output-path-detect, phase-policy, and jsonl-extract clusters into leaf modules
# under recovery_engine/; zero logic change, call sites re-imported byte-identical).
# #3479 item-2: lowered 3718 -> 3607 (behavior-preserving extraction of the
# terminal-success watcher / recovery start-offset helper cluster into the leaf
# module recovery_engine/terminal_watcher.rs; zero logic change, externally-called
# helpers re-imported byte-identical).
# #3479 item-2: lowered 3607 -> 3424 (behavior-preserving extraction of the
# inflight-state derivation helper cluster — handoff message, ready-for-input
# probes, worktree path/branch/info derivation, spawn-cwd — into the leaf module
# recovery_engine/state_extractors.rs; zero logic change, `save_missing_session_handoff`
# re-exported and root-called helpers re-imported byte-identical).
# #3479 SPC-shadow removal: lowered 3330 -> 3276 (deleted the dead StatusPanelController
# recovery-completion shadow block + assert_recovery_completion_parity fn + its dead test;
# legacy completion_target let-else and complete_status_panel_v2_with_http IO unchanged).
# #3562: +53 (3276 -> 3329) for recovery_fired/agent_quality_event turn_id+agent_id
# traceability — turn_id (recovered_transcript_turn_id) and agent_id (resolve_role_binding)
# derivation at the 3 recovery emit sites + helper signature widening; no logic change.
# #3581: +11 (3329 -> 3340) for the rebind-origin birth-site stamps — the three
# bounded-preservation fields (created_at_unix / deadline_secs / birth_generation)
# are stamped right after `turn_source = ExternalAdopted` so an unadopted,
# never-progressed rebind orphan can be reaped instead of wedging turn-start.
# #3582: +9 (3340 -> 3349) for the `set_relay_owner_kind(Watcher)` stamp (+ its
# rationale comment) on the same rebind-origin birth site. The STALL-WATCHDOG
# force-clean -> respawn path lands here with `existing_inflight = None`, so the
# synthetic row used to default to `relay_owner_kind = None` and run every later
# idle-tail / panel / routing check on the degraded bridge-owned path even though
# the respawned watcher owns the live tmux relay (the #3582 "relay re-occurrence"
# regression). No new direct-send; pure inflight-field stamp mirroring tmux.rs.
# #3610 PR-2: +33 (3349 -> 3382) for the anchor-repost backstop hook in the
# committed branch of `restore_inflight_turns` (the #3607 "committed-then-gone"
# fallback). The whole block is gated behind the default-OFF
# `recovery_anchor_repost_enabled()` flag (dark deploy — flag OFF is a byte-for-byte
# no-op), and the heavy lifting lives in the non-giant `recovery_paths/restart.rs`
# (`try_recover_anchor_repost`) — this site only resolves `tmux_alive` and disposes
# the returned outcome. No new direct-send; the repost reuses the existing
# `relay_recovered_terminal_text_to_placeholder` send-new path.
# #3610 PR-2 codex r2 (Issue-2 storm guard): +15 (3382 -> 3397) for the comment
# block at the anchor-repost dispose call documenting why `tmux_alive = false` is
# passed (committed-row repost must be budget-bounded, not pane-preserved). The
# call itself drops the now-unused `tmux_session_alive_with_retry` probe — logic
# shrank by one line; the net +15 is comment LoC explaining the storm-prevention
# invariant. No new behavior at this site beyond the literal `false` argument.
# f12b09366 (backstop missed turn intake): +24 (3397 -> 3421) for drain-restart
# ownerless-inflight recovery predicates (phase_policy/relay_recovery/relay_health).
# Hotfix was deployed via direct push to main and bypassed CI, so this baseline
# re-inflation surfaced only when PR #3655 (which bases on it) ran the gate.
# #3856 split moved TUI prompt relay external-input recovery support into this
# existing tracked surface; refresh baseline to current main after the merge.
"src/services/discord/recovery_engine.rs" = 3899
# #3034 batch-8: lowered 3771 -> 3770 (dead-code removal).
# #3038 S1: +1 (3770 -> 3771) for the mechanical `shared.queued_placeholders` ->
# `shared.queued.queued_placeholders` re-wire (one extra method-chain line) forced
# by lifting cluster C into `QueuedPlaceholderState`. Net program-wide: mod.rs -201.
# FIX #6 (Codex P2): +6 (3771 -> 3777) for the `set_followup_requeue_context`
# call that persists the originating Intervention's reply/upload/voice context on
# the follow-up inflight row so a PRE-submit busy-timeout requeue can rebuild it.
# #3557 (A): hard-ceiling clamp wired into the watchdog auto-extend block so a
# turn keeping inflight warm forever can no longer push the deadline without
# bound (Codex 13125s outlier source). +27 prod LoC. 3672 -> 3699.
# #3557 (A) codex r2 baseline correction: the prior commit froze 3699 but the
# splitter actually counts 3722 production LoC for this file (the auto-extend
# clamp + the init-time ceiling-cap block landed together, the latter undercounted
# in the prior bump). No new code lands here this round; this only corrects the
# frozen value to the file's true LoC so the ratchet gate passes. 3699 -> 3722.
"src/services/discord/router/message_handler/intake_turn.rs" = 3722
"src/services/discord/meeting_orchestrator.rs" = 3227
# #3293: +3 for the `registry_purge` child-module declaration (peek +
# operator-gated idle-entry purge live outside the frozen root); lowered
# 3090 -> 3089 to lock the net position.
# #3297 r2 (codex TOCTOU fix): the +15 actor-root pieces of the purge
# `CloseIfIdle` tombstone (msg variant + state flag + dispatch arm +
# `TryStartTurn` guard — the enum/state/loop can only live in the root)
# are offset by #2374 actor-arm comment dedup in the same root, so the
# frozen baseline stays at 3089 (no raise; #3028 ratchet).
# #3864 (P1 data-loss): move the SIGTERM queue-restore merge INSIDE the mailbox
# actor so a live reconcile-window enqueue can no longer be overwritten. Adds the
# `intervention_dedup_ids` helper + thorough (source_message_ids) dedup in
# `hydrate_pending_queue_into_state`, plus the `MergeRestoredQueueItems` actor
# message (variant + handle method + handler arm) with race-explanation comments.
# Also gates the now-test-only `ReplaceQueue` blind-overwrite path behind
# `#[cfg(test)]` (+ justification comments). Test functions are excluded from the
# count. +95 prod LoC. 3089 -> 3184.
"src/services/turn_orchestrator.rs" = 3184
# #3034 batch-8: +1 for a test-only `#[allow(dead_code)]` on
# `thread_guard_inflight_is_stale` (#1446 Layer-2 classifier, pinned by tests).
# #3038 S1: +2 (2958 -> 2960) for the two mechanical `.queued_placeholders` ->
# `.queued.queued_placeholders` re-wires (two extra method-chain lines) forced by
# the cluster-C extraction. Net program-wide: mod.rs -201.
# intake DB-error-log elevation (#3446): 2960 -> 2969.
# /steer P1.5 (PR #740): +6 for the steer-cancel message-component routing arm in
# `handle_event` (mirrors the model-picker / idle-recap arms — the dispatch can
# only live in the root event handler). The cancel handler body itself lives in
# the non-giant `steering.rs`, not here. Combined with #3446: 2969 -> 2975.
# #3576 announce-trigger restore: +1 (2977 -> 2978) for the extra
# `announce_bot_id,` argument line on the `is_allowed_turn_sender` call in
# `handle_event` (restores the announce branch removed by #3478). The call
# site lives in the root event handler; the gate logic itself stays in the
# non-giant `dispatch_policy.rs`.
# /sidecar component dispatch: +8 (2978 -> 2986) for the `is_sidecar_custom_id`
# check + handler delegation in the `InteractionCreate` arm of `handle_event`;
# the handler logic itself lives in the non-giant `sidecar_interaction.rs`.
"src/services/discord/router/intake_gate.rs" = 2986
"src/services/discord/formatting.rs" = 2802
# #3038 run_bot S0/S1: lowered 2802 -> 1918 by adding characterization tests and
# moving restored_state, queued_placeholders, startup_doctor, orphan_recovery,
# and session_gc into sub-700 `runtime_bootstrap/` modules.
# #3038 run_bot S2/S3: lowered 1918 -> 524 by moving framework_setup,
# spawns, recovery_flush, voice, gateway_lease, shutdown, and intake into
# sub-700 `runtime_bootstrap/` modules. S4 shared-data builder remains gated.
# #3038 run_bot S4: lowered 524 -> 369 by moving `run_bot_build_shared_data`
# into `runtime_bootstrap/shared_data.rs` (gated on the SharedData S1-S3
# slices, now merged). The root keeps only RunBotContext + run_bot +
# mod/re-export plumbing; final registry/baseline cleanup is the S5 slice.
# #3038 run_bot S5: lowered 369 -> 274 by moving the 120-line leader gateway
# runtime tail into `runtime_bootstrap/gateway_runtime.rs`; all
# `runtime_bootstrap/` child modules remain under the 700-line namespace cap.
"src/services/discord/runtime_bootstrap.rs" = 285
# #3034 batch-8: lowered 2655 -> 2645 (dead-code removal: `stop_provider_channel_runtime`).
# #3293: lowered 2645 -> 2641 (mailbox probe routed through the non-creating
# `health/mailbox.rs::peeked_provider_mailbox_state`).
# #3668 F2: raised 2722 -> 2733 (+11) — deliberate admission. Added a stall-
# watchdog loop-top tail-answer guard (one early-`continue`) so neither
# destructive branch (idle-clear nor desynced force-clean) drops a final answer
# still persisted in JSONL after last_offset. A correctness guard against
# permanent answer loss; splitting recovery.rs is out of scope for this fix.
# #3671: raised 2733 -> 2736 (+3) — deliberate admission. The stall-watchdog
# liveness gate now passes the turn's RAW restart-invariant age
# (`judgment_basis.turn_age_secs`, NOT the boot-floored anchor age) into
# `evaluate_stall_watchdog_liveness` so the force-clean defers indefinitely under
# positive liveness up to an age-based absolute backstop that repeated restarts
# cannot reset, instead of a brittle tick count (codex review #3672 finding). One
# new argument line plus a 2-line explanatory comment at the existing call site;
# splitting recovery.rs is out of scope.
"src/services/discord/health/recovery.rs" = 2736
# #3629: +81 (2641 -> 2722) for the NO_REPLY/empty orphan inflight cleanup in the
# completed-stale leak-detection path: the `completed_stale_no_answer_orphan_should_clean`
# pure seam (+ safety doc), the identity-guarded clear + lifecycle telemetry at the
# call site, and its unit test. P1 bugfix (#3629); the clean logic needs the
# file-private detection context so it lives here until the inflight/recovery split.
# #3293: +11 for the additive `recovery_relay_attempts` restart-budget counter
# (field + `RECOVERY_RELAY_RESTART_ATTEMPT_BUDGET`), lowered 2466 -> 2465 to
# lock the net position.
# #3351: +58 for `clear_current_msg_if_matches(_in_root)` — the relay-placeholder
# compare-and-clear needs the file-private lock/load/persist internals, so it
# lives here until the inflight split.
# FIX #6 (Codex P2): +62 (2523 -> 2585) for the five `#[serde(default)]`
# follow-up requeue context fields on `InflightTurnState` (+ doc comments), the
# v8->v9 version bump comment, the `set_followup_requeue_context` setter, and the
# matching defaults in `InflightTurnState::new`, so a PRE-submit busy-timeout
# requeue rebuilds the retry Intervention without losing reply/upload/voice data.
# #3479 Phase-2 rank-7: behaviour-preserving split — the pure domain model moved
# to inflight/model.rs (587 prod LoC) and the FS path/flock seam to
# inflight/store.rs (64 prod LoC). Both sub-1000 (not giants → no registry/doc
# entry). Lowers 2585 -> 1980 (actual post-split prod LoC). The status-panel
# guards stay in root: their compare-and-clear still needs the file-private
# load/persist-under-lock internals, so they cannot move cleanly yet.
# #3552: +55 (1980 -> 2035) for the severity-aware invariant recorder
# (`record_inflight_invariant_with_severity` + `offset_monotonic_invariant_severity`
# pure helper + doc) on the save path. When the #3416 enforce guard will skip a
# backward inflight write (offset preserved, zero data loss) the paired
# offset-monotonic invariant is recorded at WARN instead of ERROR, killing the
# duplicate ERROR-log noise; the ERROR severity is preserved for the persist path.
# This save-path emit is the SINGLE lock-atomic authority for the
# response_sent_offset_monotonic invariant (codex r2): it re-validates the row
# under its own lock and emits at the correct severity atomically with the
# skip/persist decision, so the tmux watcher pre-save emit was removed as a
# race-prone duplicate (`offset_monotonic_invariant_severity` stays private).
# #3558 raised 2035 -> 2275 (+240): two new single-flock read-modify-write helpers
# that fix the watcher offset TOCTOU — `persist_watcher_stream_progress_locked`
# (preserves the non-owned `last_offset` from the in-lock reload) and
# `commit_watcher_terminal_delivery_locked` (max-serializes the watermark) — plus
# their `WatcherStreamProgressPatch` / `WatcherTerminalCommitPatch` value structs,
# `WatcherProgressOutcome` / `WatcherTerminalCommitOutcome` enums, and 5 unit
# tests covering last_offset preservation, fresh-row identity rejection,
# backward/forward commit max-serialize, and Gemini retry-reset non-destruction.
# #3558 (codex review follow-up) raised 2275 -> 2378 (+103): a third single-flock
# RMW helper `persist_watcher_relay_watermark_locked` (with its
# `WatcherRelayWatermarkPatch` value struct and `WatcherRelayWatermarkOutcome`
# enum) for the two `tmux_watcher.rs` session-bound-relay-success sites — it
# patches ONLY `last_watcher_relayed_*` and preserves the in-lock
# `last_offset`/`response_sent_offset`/`full_response`, so a late relay can no
# longer clobber a concurrently-advanced watermark backward — plus 2 unit tests
# (concurrent-advance preservation, fresh-row identity rejection).
# #3581 raised 2378 -> 2551 (+173): bounded reap of abandoned rebind-origin
# orphans (the STALL-WATCHDOG wedge source). Adds the env-overridable deadline
# constant + `rebind_origin_deadline_secs_env` (clamped) + `now_unix`, the
# strict-conjunction predicate `should_reap_abandoned_rebind_origin` (owner-less +
# unadopted + never-progressed + never-delivered; reap iff past-deadline OR
# prior-generation), the `rebind_origin_age_secs` stamp/mtime age helper, the
# `emit_reap_abandoned_rebind_origin` #3561 lifecycle event, and the boot-time
# reap branch wired into `invalidate_stale_generation_in_root` (the prior code
# skipped every rebind row unconditionally, the permanent-orphan source). The
# bulk is load-bearing doc comments pinning the never-live argument; the tests
# live in `#[cfg(test)] mod rebind_origin_reap_tests` and are excluded here.
# #3581 (codex TOCTOU follow-up) raised 2551 -> 2660 (+109): closes the
# placeholder-sweeper reap race (unlocked-snapshot pre-check, then
# `delete_inflight_state_file` unlinked without re-reading the row under the
# lock — a racing intake/TUI claim could persist a new live inflight at the same
# sidecar path and have it wiped). Adds the shared locked re-validate helper
# `reap_abandoned_rebind_origin_locked_in_root` (+ its env wrapper
# `reap_abandoned_rebind_origin_locked`), the `RebindReapOutcome` enum, and the
# `rebind_row_identity_unchanged` identity guard (created_at + birth_generation +
# turn_start_offset) so a replacement turn is never mistaken for the snapshotted
# orphan. Both the sweeper and the boot `invalidate_stale_generation_in_root`
# path now route their unlink through this single helper (the boot branch loses
# its inline lock+reload). The bulk is load-bearing doc comments; the 4 new
# boundary tests live in the excluded `rebind_origin_reap_tests` module.
# #3641 raised 2660 -> 2771 (+111): boot-time orphan `.json.lock` sweep for
# `discord_inflight/{provider}` sidecars whose matching `.json` row is gone.
# Adds the 1h conservative age floor, lock-name/base-path helpers, root-explicit
# testable sweep, runtime-root wrapper, and boot hook. Tests live in the excluded
# `orphan_lock_reap_tests` module.
# #3635 raised 2771 -> 3052 (+281): dead-watcher rebind-origin reap. A
# Watcher-owned rebind orphan (the #897 birth shape, `relay_owner_kind =
# Watcher`) can never satisfy the existing `should_reap_abandoned_rebind_origin`
# `== None` owner conjunct, so it leaked on disk forever after the watcher died.
# This adds a SEPARATE gate that leaves the byte-for-byte-unchanged None-owner
# predicate (and its 19 live-protection pinning tests) intact: the new
# `WatcherLiveness` DI trait + `RuntimeWatcherLiveness` operational oracle
# (three-state tmux pane liveness + dispatched-sessions runtime-activity age,
# reused from #3169 / #3629), the Watcher-owner structural predicate, and the
# `sweep_reap_dead_watcher_rebind_origin` warm sweeper entry point. Codex review
# follow-up removed the bool-collapse bug (`ProbeError` is unknown/preserve), runs
# tmux subprocess probes outside the sidecar lock on `spawn_blocking` with a
# timeout, and keeps the locked helper to cheap fs-only identity/structure plus
# recent-runtime-activity rechecks. recovery_engine.rs (#3016 birth) is 0 lines
# changed; only the warm placeholder-sweeper carries the liveness gate (never the
# boot path) so a freshly-restarted live watcher is never false-classified as dead.
# Tests live in the excluded `rebind_origin_reap_tests` module.
"src/services/discord/inflight.rs" = 3138 # #3680 relay recovery review hardening / adoption guards
# #3779 extracted relay-recovery auto-heal attempt accounting to a sibling
# module, dropping relay_recovery.rs below the prod-giant threshold; the ratchet
# entry is retired instead of raising the baseline.
# #3038 Phase A: health.rs decomposed into health/ directory modules (root 402
# prod LoC + 6 sub-1000-LoC submodules) — dropped below the giant threshold, so
# its ratchet entry is deleted (win; no baseline raises).
# #3034 batch-8: lowered 2333 -> 2332 (dropped `assistant_turns` construction site).
"src/services/discord/watchers/lifecycle.rs" = 2332
# #3479: idle_recap.rs decomposed (scrollback + context_display submodules) —
# dropped below the giant threshold, so its ratchet entry is deleted (win; no
# baseline raises).
# #3034 batch-8: lowered 2241 -> 2011 (deleted the dead #826/#897/#898 bg-trigger
# notify-outbox subsystem — build/enqueue/reconcile/parse/rollback).
# suppressed-label noise, user report 2026-06-12: +38 for provider-aware
# status/footer stripping in the placeholder suppression decisions.
# #3552 (codex r2): +1 (2057 -> 2058) — the racy pre-save
# `response_sent_offset_monotonic` severity emit added in #3552 codex r1 is
# REMOVED. It computed WARN/ERROR against an unlocked `load_inflight_state`
# snapshot, but the immediately-following `save_inflight_state` re-judges the row
# under its own lock; a concurrent clear/fresh-row between them could make the
# tmux emit WARN while the save actually persists a stale backward write (a
# WARN-only-when-skipped breach hidden). `persist_watcher_stream_progress` always
# reaches that save (no early return after the check), so the lock-atomic
# save-path emit is the single authoritative source — the tmux emit was a pure
# duplicate. Dropping it also removes `record_watcher_invariant_with_severity`,
# `persist_watcher_response_sent_offset_severity`, and the
# `persist_watcher_stream_progress_with_authority` test seam; net +1 vs base is a
# replacement doc-comment explaining the delegation. The DEBUG-only
# `debug_assert!(monotonic_offset)` tripwire stays (production no-op).
# #3558 lowered 2058 -> 2049 (-9): `persist_watcher_stream_progress` dropped its
# unlocked `load_inflight_state` -> mutate -> `save_inflight_state` body and now
# builds a `WatcherStreamProgressPatch` + delegates the read-modify-write to the
# single-flock `inflight::persist_watcher_stream_progress_locked`. Locks in the
# shrink win.
"src/services/discord/tmux.rs" = 2049
# completion_guard.rs decomposed in #3479 (1834 -> 872 prod LoC); dropped below
# the giant threshold, so its ratchet baseline is retired (no longer tracked).
# session_runtime.rs decomposed in #3842 (worktree, restore_cwd, and
# channel_routing child modules); dropped below the giant threshold, so its
# ratchet baseline is retired.
# #3034 batch-8: +1 for a test-only `#[allow(dead_code)]` on
# `session_bound_terminal_delivery_route_decision` (#3041 route decision, tested).
# #3610 PR-1: 1731 -> 1738 — pass terminal anchor (current_msg_id) explicitly to
# shadow_mirror_delivered_frontier at the sink delivery call site (Phase B prereq;
# irreducible 6-arg rustfmt-wrapped call). panel_msg_id has 0 prod readers -> dedup unchanged.
"src/services/discord/session_relay_sink.rs" = 1738
# #3635: placeholder_sweeper.rs crossed the 1000-prod-LoC giant threshold (993 ->
# 1004) when the dead-watcher rebind-origin reap branch was added to the
# rebind-origin arm of the sweep loop (the watcher-owned #897 orphan the #3581
# None-owner predicate can never touch) and then awaited the async, spawn_blocking
# dead-watcher probe helper. Registered as a tracked [[entry]] in
# scripts/giant_file_registry.toml (discord-relay, decompose_issue #3405). The
# reap-eligibility logic lives in inflight.rs; this arm is the orchestration shell.
# Frozen here so future sweep-arm growth is reviewable.
# #3859: 1004 -> 1014 (+10 production LoC) — the sweep loop now drains the new
# abandon_request_store each pass, finalizing failure-path-stranded placeholders
# to "중단됨" by message id (decoupled from the inflight lifecycle). Bugfix-only.
"src/services/discord/placeholder_sweeper.rs" = 1014
# #3479: turn_bridge/tmux_runtime.rs decomposed into tmux_runtime/ directory
# modules (root tmux_runtime.rs 964 prod LoC + sub-1000-LoC
# tmux_runtime/{interrupt_policy,process_table,pid_exit}.rs) — dropped below the
# giant threshold, so its ratchet entry is deleted (win; no baseline raises).
"src/services/discord/commands/text_commands.rs" = 1493
# terminal_delivery.rs dropped to 504 prod LoC after the #3028 splitter fix
# (its inline test mod was previously miscounted as production), so it is no
# longer a giant and its ratchet/registry entries were removed.
# #3034 batch-8: +2 for the `#[allow(dead_code)]` on the `RelayMiss` terminal-event
# variant + its reason comment (handled by the finalizer + tested, not yet emitted
# in prod; rustfmt keeps the attribute and the comment on separate lines).
# #3479 r9: 1528 -> 1337 (-191) by extracting the completion-signal enum + pure
# derivation (turn_finalizer/completion_signal.rs), the dormant delivery-lease
# handlers (turn_finalizer/delivery_lease.rs), and the watcher far-backstop
# tunables + terminal-or-defer verdict pair (turn_finalizer/watcher_backstop.rs)
# into child modules. Pure move; each child is well under the 1000-prod-LoC
# giant threshold so no new ratchet/registry entry is needed.
# #3646 raised 1337 -> 1364 (+27 prod LoC): OBSERVATION-ONLY finalizer_ledger_owner
# companion emit + the `terminal_event_kind_str` wire-string helper. The actor-owned
# ledger entry's `relay_owner` is the SECOND owner signal the watcher cannot read
# synchronously; it is emitted (keyed on the same turn_id) right after the Phase gate
# so it JOINs the watcher terminal_body_commit in PG. Read-only: it does NOT inspect or
# change the clear_inflight / defer / finalize decision. Resolves #3607 owner ambiguity.
# #3646 codex review #3678: 1364 -> 1373 (+9 prod LoC): key the finalizer_ledger_owner
# emit on the RESOLVED entry identity (`entry.turn_key`) instead of the submitted `key`,
# so id-0 terminals that collapse onto a real registered turn keep the real turn_id and
# JOIN correctly against the watcher event (the #3607 cases the signal exists for). Plus
# the clarifying rationale comment. Still read-only; no decision-path change.
# #3866: raised 1373 -> 1521 (+148 prod LoC; test module excluded): contained
# per-message panics on the reconcile/backstop finalize path — catch_unwind on the
# reconcile timer arm + do_finalize guards at both call sites (handle_terminal and
# run_backstop_finalize) so a panic can no longer kill the single never-respawned
# finalizer actor — plus the residual-risk documentation. Deliberate, reviewable
# baseline raise for a SAFETY fix; decomposing the file back under baseline (pure
# move into a turn_finalizer/ child, cross-reviewed) is deferred to #3894 to avoid
# a risky core-hotfile refactor mid-stabilization.
# #3894: 1521 -> 1038 (-483 prod LoC) by extracting three cohesive clusters
# VERBATIM (pure move, zero logic change) into turn_finalizer/ child modules —
# the per-submission finalize context (turn_finalizer/finalize_context.rs), the
# finalize side-effect chokepoint `do_finalize` (turn_finalizer/finalize.rs),
# and the timer-driven reconcile/backstop cluster `reconcile` +
# `run_backstop_finalize` (turn_finalizer/reconcile.rs). The only edits to the
# moved bodies are mechanical: re-export/re-import so call sites stay
# byte-identical, widening `FinalizeContext::gate_backstop` to `pub(super)`, and
# rewriting bare `super::` discord-module references to absolute
# `crate::services::discord::` paths because the module depth changed. Each child
# is well under the 1000-prod-LoC giant threshold (tracked below at landed LoC).
"src/services/discord/turn_finalizer.rs" = 1038
# #3894: the three new turn_finalizer/ child modules landed below the 1000-prod-LoC
# giant threshold; tracked here at their landed production LoC so the decomposition
# cannot silently re-inflate any child toward giant-hood (the ratchet only fails
# once a file actually crosses the giant threshold, so these freeze them sub-giant).
"src/services/discord/turn_finalizer/finalize.rs" = 208
"src/services/discord/turn_finalizer/finalize_context.rs" = 106
"src/services/discord/turn_finalizer/reconcile.rs" = 220
# #3557 (A) codex r2: the headless watchdog was missing the per-turn hard
# ceiling cap that the foreground intake path already had — and it also
# `mark_async_managed`s the token (sync watchdog stops enforcing), so this async
# loop is the ONLY bound. Added the initial-deadline `min(now+timeout, ceiling)`
# cap + one-shot ceiling warn and the auto-extend `clamp_auto_extend_deadline_ms`
# clamp (reusing the shared discord/mod.rs helpers). +54 prod LoC (the new test
# module is excluded from the production count). 1462 -> 1516.
# #family-profile-probe: raised 1516 -> 1543 (+27 prod LoC; the dm_fresh_routine
# test module is excluded). `dm_fresh_routine_turn` discriminator routes a fresh
# DM routine turn through the shared `/goal fresh` machinery (force_fresh =
# goal_fresh || dm_fresh): thorough clear + Claude TUI runtime-binding clear +
# DB/live restore skip + launch fresh flag — closing all four session-reuse
# layers (in-memory, DB, Codex wrapper, Claude TUI runtime-binding recovery)
# codex review P1/R2/R3. Bugfix wiring at the existing session-load/restore/
# launch site; splitting headless_turn.rs is out of scope.
"src/services/discord/router/message_handler/headless_turn.rs" = 1543
# #3038 S2: lowered 1054 -> 954 (-100) as the session-override bookkeeping
# helpers (fast-mode reset-entry codec + reset-pending probes/clears +
# enablement probes + sync_session_reset_pending) moved verbatim to the
# `shared_state` sibling module alongside `SessionOverrideState`. config.rs is
# no longer a prod-giant; the entry stays ratcheted at the landed value so a
# re-expansion past it fails the gate (runtime_bootstrap.rs precedent, #3038).
"src/services/discord/commands/config.rs" = 954
# #3479: diagnostics.rs decomposed into commands/diagnostics/ directory modules
# (root commands/diagnostics/mod.rs 389 prod LoC + sub-1000-LoC
# commands/diagnostics/reports.rs) — dropped below the giant threshold, so its
# ratchet entry is deleted (win; no baseline raises).

# #3038 slice 1: decision_route.rs (4392 prod LoC) was decomposed into a
# 139-line module root + 8 directory modules. #3038 review-decision S1 lowers
# the route root 139 -> 26 by moving the orchestrator and verbatim phase
# modules into services::review_decision; the service root and child modules
# are frozen at their landed production LoC to prevent re-inflation.
"src/server/routes/review_verdict/decision_route.rs" = 26
"src/services/review_decision.rs" = 335
"src/services/review_decision/accept.rs" = 703
"src/services/review_decision/adapters.rs" = 226
"src/services/review_decision/dismiss_finalize.rs" = 182
"src/services/review_decision/dispute.rs" = 746
"src/services/review_decision/pending.rs" = 636
"src/services/review_decision/repo_card.rs" = 748
"src/services/review_decision/repo_dispatch.rs" = 650
"src/services/review_decision/review_state_repo.rs" = 35
"src/services/review_decision/tuning_aggregate.rs" = 469
"src/services/review_decision/worktree_stale.rs" = 553
