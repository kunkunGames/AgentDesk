# Configuration for scripts/audit_maintainability.py.
#
# Namespace caps apply to production Rust files under src/. Test-only Rust
# files remain excluded by the shared production_rust_files() filter.

[namespace_size_caps]
"src/services/discord/commands/inspect/**" = 700
"src/db/prompt_manifests/**" = 700
# 2026-07-06 #4091 admission: +3 for the crate-wide env-lock test-isolation sweep —
# race_loss.rs's `#[cfg(test)]` module gained `shared_test_env_lock()` acquisition +
# `#[allow(clippy::await_holding_lock)]` to serialize AGENTDESK_ROOT_DIR mutation.
# Test-harness lines only; production logic unchanged. More specific than the `**`
# glob below, so it must precede it for fnmatch first-match precedence.
"src/services/discord/router/message_handler/intake_turn/race_loss.rs" = 704 # 2026-07-06 #4183 CI-red reconcile: 703 -> 704 (+1 prod LoC drift from a recent landing; masked behind the hotfile-ratchet set -e exit until #4183 fixed it); admission to unblock main red.
# #3837: child modules carved out of the intake_turn.rs giant via behavior-
# preserving decomposition (voice_intake / race_loss / turn_watchdog). Cap holds
# the new surface under the 700-line namespace ceiling.
"src/services/discord/router/message_handler/intake_turn/**" = 700
# #3811: status_panel.rs was already at 696/700 (effectively full). The turn-anchor
# feature needs the irreducible minimum on this surface — the `request_user_msg_id`
# snapshot field, its preserve/clear reset hooks, and the `render_status_panel`
# anchor-prepend param (the heavier setter/line-builder/store helpers live in the
# new turn_anchor.rs). More specific than the `**` glob below, so it must precede it
# for fnmatch first-match precedence.
"src/services/discord/placeholder_live_events/status_panel.rs" = 794 # 2026-07-08 #4177: 738 -> 794 (+56), reviewable admission — store subagent agentId metadata, safely fallback-pair genuine async completions, and trim finished slots first.
"src/services/discord/placeholder_live_events/status_events.rs" = 794 # 2026-07-08 #4338: 782 -> 794 (+12), reviewable admission — decode harness XML entities once at the footer-mode task-notification bridge (codex r1 finding 2); prior #4177 r2: 779 -> 782 (+3), restore parent_tool_use_id doc placement and add task_notification_agent_id doc; prior #4177: 720 -> 779 (+59), carry task-notification/toolUseResult agentId and description metadata into SubagentEnd fallback matching.
"src/services/discord/placeholder_live_events/**" = 700
"src/services/discord/prompt_builder/**" = 700
"src/services/discord/runtime_bootstrap/**" = 700
"src/services/discord/tmux_watcher/streaming_status_tick.rs" = 974 # 2026-07-08 #4229 S3: approved contiguous verbatim extraction of the throttled streaming status tick (orphan reclaim/suppression/panel create-bind/rollover/re-anchor/placeholder edit); below giant threshold, but irreducible as one review-verified move. 2026-07-22 #4104 dual-review admission 963 -> 974: the throttled tick now performs a full identity-guarded durable inflight persist so no-edit/silent tool-hold progress reaches the durable row (fixes E-22 misdiagnosing a stale snapshot as a mid-turn delete). The +11 is the identity-guard load + durable persist call site, irreducible without re-aliasing the tick's inflight snapshot locals.
"src/services/discord/tmux_watcher/turn_stream_collector.rs" = 1161 # 2026-07-08 #4229 S4: approved contiguous verbatim extraction of the turn stream collector (seed restore/first parse/forward/monitor auto-turn claim/active read-parse loop); giant file (>=1000), registered in giant_file_registry.toml; irreducible as one review-verified move.
"src/services/discord/tmux_watcher/no_result_exits.rs" = 814 # 2026-07-08 #4229 S6: approved contiguous verbatim extraction of the no-result exit handler (fresh ready-for-input idle structural finalize / tmux-death break / cancel-shutdown break / ready-for-input stall notice+dispatch-fail / #3419 watchdog-timeout finalize); below giant threshold (<1000), but irreducible as one review-verified move.
"src/services/discord/tmux_watcher/terminal_direct_fallback.rs" = 730 # 2026-07-23 #4822 (PR #4826) reviewable admission 698 -> 730 (+32), dual-review (5 rounds) CLEAN — footer usage-format completion-footer terminal target wiring: the two long-chunks controller call sites thread `single_message_panel_footer_mode` + `completion_footer_terminal_target` into the shared ctx, and the fallback-after-edit-failure / placeholderless fresh-send delivery arms call `remember_watcher_completion_footer_terminal_target` with the split_message tail so the completion footer can rebind to the actually delivered terminal message. Irreducible without re-aliasing the delivery arms' anchor/relay_text locals; real formatter logic lives in terminal_status_formatting.rs / single_message_footer.rs.
"src/services/discord/tmux_watcher/**" = 700
"src/services/discord/turn_bridge/headless_delivery.rs" = 700
"src/services/discord/turn_bridge/activity_heartbeat.rs" = 77 # 2026-07-10 #4230 S11 landed production cap for the active-turn tmux activity-heartbeat helpers.
"src/services/discord/turn_bridge/stream_receiver.rs" = 109 # 2026-07-10 #4230 S11 landed raw-LoC cap for the blocking-to-async stream receiver adapter, wait policy, and their inline tests.
"src/services/discord/turn_bridge/thinking.rs" = 87 # 2026-07-10 #4230 S11 landed raw-LoC cap for the redacted thinking helpers and their inline tests; split from content_arms because its existing cap could not be raised.
"src/services/discord/turn_bridge/terminal_outcome_delivery.rs" = 890 # 2026-07-10 #4230 S10: lowered 1370 -> 882 after extracting the recovery fallback, empty-response/silent-turn arm, and post-commit epilogue; no longer a prod giant. 2026-07-17 #4605 reviewable-admission 882 -> 887: dual-review safety fixes (busy readiness-timeout no longer H5-misclassified, output-file recovery blocked for busy follow-up, requeue failure preserved instead of reported as success) added the two-helper caller glue; recovery handler moved to empty_response_recovery/handler.rs, residual +5 is the sequential requeue call site that cannot fold without re-aliasing the shared mutable locals. 2026-07-17 #4610 reviewable-admission 887 -> 888: dual-review found the epilogue requeue gate `!committed && !preserve` killed the legacy non-busy requeue path (silent message loss); threading `claude_tui_busy_requeue_pending` through the epilogue ctx literal is the irreducible +1 to discriminate busy (already requeued) from legacy (must requeue). 2026-07-19 #4564 reviewable-admission 888 -> 890: channel-split silent-loss fix — the completed-turn ledger must key by the DELIVERY channel (`channel_id`) and record the delivered turn's inbound `user_msg_id`, NOT reload the offset-authority `watcher_owner_channel_id` whose preserved inflight row is an UNANSWERED turn. The irreducible +2 is the two call-site args threading `inflight_state.user_msg_id` into `apply_bridge_long_chunks_legacy` and `record_delivered_frontier_with_body` from the turn snapshot; the value cannot be derived without the snapshot.
"src/services/discord/turn_bridge/terminal_outcome_delivery/cancel_prompt_replace.rs" = 447 # 2026-07-10 #4230 S9 approved contiguous extraction of cancelled/prompt-too-long terminal replacement arms with both #3041 delivery-lease acquire/commit pairs intact.
"src/services/discord/turn_bridge/terminal_outcome_delivery/delivery_epilogue.rs" = 432 # 2026-07-10 #4230 S10 landed production cap for the post-commit epilogue. 2026-07-17 #4610 reviewable-admission 430 -> 432: the busy-vs-legacy requeue gate fix adds the `claude_tui_busy_requeue_pending` ctx field + its destructure (irreducible thread) plus a one-line gate rationale.
"src/services/discord/turn_bridge/terminal_outcome_delivery/empty_response_recovery.rs" = 419 # 2026-07-10 #4230 S10-b landed production cap for empty-response recovery and silent-turn delivery with its #3041 lease pair intact.
"src/services/discord/turn_bridge/terminal_outcome_delivery/recovery_retry.rs" = 103 # 2026-07-10 #4230 S10 landed production cap for the recovery-retry fallback.
"src/services/discord/turn_bridge/stream_loop.rs" = 979 # 2026-07-10 #4230 S8: lowered after approved extraction of RetryBoundary/Init/Text/Thinking/Done/Error/StatusUpdate/StatusEvents arms to stream_loop/content_arms.rs; below the giant threshold.
"src/services/discord/turn_bridge/stream_loop/content_arms.rs" = 635 # 2026-07-10 #4230 S8 approved extraction of content/status/terminal stream-loop arms, including explicit Text drain-continue outcome mapping.
"src/services/discord/turn_bridge/stream_loop/tool_arms.rs" = 684 # 2026-07-10 #4230 S7 approved contiguous verbatim extraction of ToolUse/ToolResult/TaskNotification stream-loop arms.
"src/services/discord/turn_bridge/status_panel.rs" = 700
"src/services/discord/turn_bridge/voice_completion.rs" = 700

# Frozen test-residue ceilings for the parent_test_residue check (#4267).
#
# When a giant file is decomposed its production code moves out but its inline
# `#[cfg(test)]` blocks often stay behind, leaving a "test graveyard" whose
# stranded test LoC dwarfs its remaining production LoC. The check flags files
# whose test/prod ratio exceeds 3x (and that are >= 500 raw LoC). It is
# report-only with a no-regression baseline: each value below is the file's
# CURRENT stranded test LoC, frozen so CI is green on introduction. The gate
# fails only when a NEW graveyard appears or one of these grows past its
# ceiling. Lower a ceiling as tests migrate out (the fix this check wants);
# RAISING a ceiling admits more residue and is forbidden (#4269) — migrate the
# tests with the production code instead.
[parent_test_residue]
# The #4267 exemplar: inflight.rs kept its ~5.4k-line inline test module after
# its production logic was carved into the inflight/ children (ratio ~9.2x).
# Tracked for migration alongside the #4201 decomposition.
"src/services/discord/inflight.rs" = 5355
# turn_finalizer.rs is itself a production giant (1048 prod LoC) carrying a
# 3992-line inline test module (ratio ~3.8x). Freeze pending test migration.
"src/services/discord/turn_finalizer.rs" = 3992
# save_store.rs carries 730 test LoC over 235 prod LoC (ratio ~3.1x) after the
# inflight/ save-path carve-out; tests to migrate with the store split.
"src/services/discord/inflight/save_store.rs" = 730
# codex_tui_replay.rs: 711 test LoC over 233 prod LoC (ratio ~3.0x) left in the
# recovery_engine manual-rebind carve-out.
"src/services/discord/recovery_engine/manual_rebind/codex_tui_replay.rs" = 711
# cancel_finalize_policy.rs: 450 test LoC over 131 prod LoC (ratio ~3.4x) in the
# turn_bridge cancel/finalize policy carve-out.
"src/services/discord/turn_bridge/cancel_finalize_policy.rs" = 450
# delivery_lease.rs: 428 test LoC over 87 prod LoC (ratio ~4.9x) in the
# turn_finalizer delivery-lease carve-out.
"src/services/discord/turn_finalizer/delivery_lease.rs" = 428
